{
  "openapi": "3.0.0",
  "info": {
    "title": "Instantly Service",
    "description": "Cold email outreach service via Instantly.ai API. Manages campaigns, leads, accounts, analytics, and webhook events.",
    "version": "1.0.0"
  },
  "servers": [
    {
      "url": "http://localhost:3011"
    }
  ],
  "components": {
    "schemas": {
      "RootResponse": {
        "type": "object",
        "properties": {
          "service": {
            "type": "string"
          },
          "version": {
            "type": "string"
          }
        },
        "required": [
          "service",
          "version"
        ]
      },
      "HealthResponse": {
        "type": "object",
        "properties": {
          "status": {
            "type": "string"
          },
          "service": {
            "type": "string"
          }
        },
        "required": [
          "status",
          "service"
        ]
      },
      "WebhookConfigResponse": {
        "type": "object",
        "properties": {
          "webhookUrl": {
            "type": "string",
            "format": "uri"
          }
        },
        "required": [
          "webhookUrl"
        ]
      },
      "Error": {
        "type": "object",
        "properties": {
          "error": {
            "type": "string"
          }
        },
        "required": [
          "error"
        ]
      },
      "WebhookResponse": {
        "type": "object",
        "properties": {
          "success": {
            "type": "boolean"
          },
          "eventType": {
            "type": "string",
            "nullable": true,
            "description": "Null when the payload carried no event_type"
          },
          "bronzeRowId": {
            "type": "string",
            "nullable": true
          },
          "promoted": {
            "type": "boolean"
          },
          "degraded": {
            "type": "boolean",
            "description": "True when the event could not be fully ingested. The endpoint still answers 200 — Instantly disables a webhook on repeated non-2xx"
          },
          "degradedReason": {
            "type": "string",
            "nullable": true,
            "description": "invalid_payload | missing_campaign_id | unknown_campaign_id | campaign lookup failed: … | bronze failed: … | silver failed: …"
          }
        },
        "required": [
          "success",
          "eventType",
          "bronzeRowId",
          "promoted",
          "degraded",
          "degradedReason"
        ]
      },
      "WebhookPayload": {
        "type": "object",
        "properties": {
          "event_type": {
            "type": "string"
          },
          "campaign_id": {
            "type": "string"
          },
          "lead_email": {
            "type": "string"
          },
          "email_account": {
            "type": "string"
          },
          "timestamp": {
            "type": "string"
          },
          "step": {
            "type": "integer"
          },
          "variant": {
            "type": "integer"
          }
        },
        "required": [
          "event_type"
        ]
      },
      "SelfSendDispatchAccepted": {
        "type": "object",
        "properties": {
          "accepted": {
            "type": "boolean"
          }
        },
        "required": [
          "accepted"
        ]
      },
      "SelfSendDispatchRequest": {
        "type": "object",
        "properties": {
          "limit": {
            "type": "integer",
            "description": "Cap the batch. Omit to send everything currently due."
          }
        }
      },
      "SendResponse": {
        "type": "object",
        "properties": {
          "success": {
            "type": "boolean"
          },
          "campaignId": {
            "type": "string",
            "nullable": true
          },
          "leadId": {
            "type": "string",
            "nullable": true
          },
          "added": {
            "type": "number"
          },
          "stepRuns": {
            "type": "array",
            "items": {
              "type": "object",
              "properties": {
                "step": {
                  "type": "integer",
                  "minimum": 1
                },
                "runId": {
                  "type": "string"
                }
              },
              "required": [
                "step",
                "runId"
              ]
            }
          }
        },
        "required": [
          "success",
          "added"
        ]
      },
      "SendRefusal": {
        "type": "object",
        "properties": {
          "error": {
            "type": "string"
          },
          "code": {
            "type": "string",
            "enum": [
              "recent_brand_contact",
              "lead_id_conflict"
            ]
          },
          "details": {
            "type": "string"
          },
          "brandId": {
            "type": "string",
            "description": "recent_brand_contact only — the brand already contacted"
          },
          "lastEmailedAt": {
            "type": "string",
            "description": "recent_brand_contact only — when the previous email to this person for that brand went out"
          },
          "windowInterval": {
            "type": "string",
            "description": "recent_brand_contact only — the re-contact window, e.g. \"3 months\""
          }
        },
        "required": [
          "error",
          "code",
          "details"
        ]
      },
      "SendRequest": {
        "type": "object",
        "properties": {
          "leadId": {
            "type": "string",
            "description": "External lead ID from lead-service"
          },
          "to": {
            "type": "string"
          },
          "firstName": {
            "type": "string"
          },
          "lastName": {
            "type": "string"
          },
          "company": {
            "type": "string"
          },
          "variables": {
            "type": "object",
            "additionalProperties": {
              "type": "string"
            }
          },
          "timezone": {
            "type": "string",
            "description": "Recipient's IANA timezone (e.g. America/New_York). Sets the Instantly campaign sending-schedule timezone so business-hours sends land in the lead's local time. Absent/invalid → America/Chicago default."
          },
          "subject": {
            "type": "string",
            "description": "Shared subject for all steps in the sequence"
          },
          "bcc": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Optional BCC recipients — set as the created campaign's bcc_list so every step of the sequence BCCs these addresses. Absent/empty = no BCC."
          },
          "sequence": {
            "type": "array",
            "items": {
              "type": "object",
              "properties": {
                "step": {
                  "type": "integer",
                  "minimum": 1,
                  "description": "1-based ordinal step number"
                },
                "bodyHtml": {
                  "type": "string",
                  "description": "HTML body for this step"
                },
                "daysSinceLastStep": {
                  "type": "integer",
                  "minimum": 0,
                  "description": "Delay in days since the previous step (0 = immediate)"
                }
              },
              "required": [
                "step",
                "bodyHtml",
                "daysSinceLastStep"
              ]
            },
            "minItems": 1,
            "description": "Ordered email steps — at least one required"
          }
        },
        "required": [
          "to",
          "subject",
          "sequence"
        ]
      },
      "UpdateStatusRequest": {
        "type": "object",
        "properties": {
          "status": {
            "type": "string",
            "enum": [
              "active",
              "paused",
              "completed"
            ]
          }
        },
        "required": [
          "status"
        ]
      },
      "ReconcileAccepted": {
        "type": "object",
        "properties": {
          "runId": {
            "type": "string",
            "format": "uuid",
            "description": "Opaque identifier for log correlation"
          },
          "startedAt": {
            "type": "string",
            "description": "ISO timestamp when the job was dispatched"
          }
        },
        "required": [
          "runId",
          "startedAt"
        ]
      },
      "StatsResponse": {
        "type": "object",
        "properties": {
          "recipientStats": {
            "type": "object",
            "properties": {
              "contacted": {
                "type": "number",
                "description": "Leads added to a campaign / send attempted"
              },
              "sent": {
                "type": "number",
                "description": "Leads with at least 1 email sent (COUNT DISTINCT lead)"
              },
              "delivered": {
                "type": "number",
                "description": "Leads delivered (sent - bounced)"
              },
              "opened": {
                "type": "number",
                "description": "Leads who opened at least 1 email"
              },
              "bounced": {
                "type": "number",
                "description": "Leads with at least 1 bounce"
              },
              "clicked": {
                "type": "number",
                "description": "Leads who clicked at least 1 link"
              },
              "unsubscribed": {
                "type": "number",
                "description": "Leads who unsubscribed"
              },
              "notSending": {
                "type": "number",
                "description": "Leads in campaigns currently flagged with Instantly's not_sending_status diagnostic. Optional in contract v1; will be required in v2 after postmark padding ships."
              },
              "cancelled": {
                "type": "number",
                "description": "Leads whose campaign was cancelled by the retry-stuck job (delivery_status='cancelled'). Optional in contract v1; will be required in v2 after postmark padding ships."
              },
              "repliesPositive": {
                "type": "number",
                "description": "interested + meetingBooked + closed"
              },
              "repliesNegative": {
                "type": "number",
                "description": "notInterested + wrongPerson + unsubscribe"
              },
              "repliesNeutral": {
                "type": "number",
                "description": "neutral (lead_neutral events only)"
              },
              "repliesAutoReply": {
                "type": "number",
                "description": "autoReply + outOfOffice"
              },
              "repliesDetail": {
                "type": "object",
                "properties": {
                  "interested": {
                    "type": "number",
                    "description": "lead_interested events"
                  },
                  "meetingBooked": {
                    "type": "number",
                    "description": "lead_meeting_booked events"
                  },
                  "closed": {
                    "type": "number",
                    "description": "lead_closed events"
                  },
                  "notInterested": {
                    "type": "number",
                    "description": "lead_not_interested events"
                  },
                  "wrongPerson": {
                    "type": "number",
                    "description": "lead_wrong_person events"
                  },
                  "unsubscribe": {
                    "type": "number",
                    "description": "lead_unsubscribed events"
                  },
                  "neutral": {
                    "type": "number",
                    "description": "lead_neutral events"
                  },
                  "autoReply": {
                    "type": "number",
                    "description": "auto_reply_received events"
                  },
                  "outOfOffice": {
                    "type": "number",
                    "description": "lead_out_of_office events"
                  }
                },
                "required": [
                  "interested",
                  "meetingBooked",
                  "closed",
                  "notInterested",
                  "wrongPerson",
                  "unsubscribe",
                  "neutral",
                  "autoReply",
                  "outOfOffice"
                ],
                "description": "Granular reply breakdown by classification"
              }
            },
            "required": [
              "contacted",
              "sent",
              "delivered",
              "opened",
              "bounced",
              "clicked",
              "unsubscribed",
              "repliesPositive",
              "repliesNegative",
              "repliesNeutral",
              "repliesAutoReply",
              "repliesDetail"
            ]
          },
          "emailStats": {
            "type": "object",
            "properties": {
              "sent": {
                "type": "number",
                "description": "Total emails sent (COUNT *, all steps)"
              },
              "delivered": {
                "type": "number",
                "description": "Total emails delivered (sent - bounced)"
              },
              "opened": {
                "type": "number",
                "description": "Unique emails opened at least once (COUNT DISTINCT)"
              },
              "clicked": {
                "type": "number",
                "description": "Unique emails with at least 1 click (COUNT DISTINCT)"
              },
              "bounced": {
                "type": "number",
                "description": "Total emails bounced"
              },
              "unsubscribed": {
                "type": "number",
                "description": "Total unsubscribe events"
              },
              "stepStats": {
                "type": "array",
                "items": {
                  "type": "object",
                  "properties": {
                    "step": {
                      "type": "number",
                      "description": "Step number (1-based)"
                    },
                    "sent": {
                      "type": "number",
                      "description": "Emails sent at this step"
                    },
                    "delivered": {
                      "type": "number",
                      "description": "Delivered at this step (sent - bounced)"
                    },
                    "opened": {
                      "type": "number",
                      "description": "Emails opened at this step"
                    },
                    "clicked": {
                      "type": "number",
                      "description": "Link clicks at this step"
                    },
                    "bounced": {
                      "type": "number",
                      "description": "Emails bounced at this step"
                    },
                    "unsubscribed": {
                      "type": "number",
                      "description": "Unsubscribes at this step"
                    },
                    "repliesPositive": {
                      "type": "number",
                      "description": "interested + meetingBooked + closed"
                    },
                    "repliesNegative": {
                      "type": "number",
                      "description": "notInterested + wrongPerson + unsubscribe"
                    },
                    "repliesNeutral": {
                      "type": "number",
                      "description": "neutral (lead_neutral events only)"
                    },
                    "repliesAutoReply": {
                      "type": "number",
                      "description": "autoReply + outOfOffice"
                    },
                    "repliesDetail": {
                      "type": "object",
                      "properties": {
                        "interested": {
                          "type": "number",
                          "description": "lead_interested events"
                        },
                        "meetingBooked": {
                          "type": "number",
                          "description": "lead_meeting_booked events"
                        },
                        "closed": {
                          "type": "number",
                          "description": "lead_closed events"
                        },
                        "notInterested": {
                          "type": "number",
                          "description": "lead_not_interested events"
                        },
                        "wrongPerson": {
                          "type": "number",
                          "description": "lead_wrong_person events"
                        },
                        "unsubscribe": {
                          "type": "number",
                          "description": "lead_unsubscribed events"
                        },
                        "neutral": {
                          "type": "number",
                          "description": "lead_neutral events"
                        },
                        "autoReply": {
                          "type": "number",
                          "description": "auto_reply_received events"
                        },
                        "outOfOffice": {
                          "type": "number",
                          "description": "lead_out_of_office events"
                        }
                      },
                      "required": [
                        "interested",
                        "meetingBooked",
                        "closed",
                        "notInterested",
                        "wrongPerson",
                        "unsubscribe",
                        "neutral",
                        "autoReply",
                        "outOfOffice"
                      ],
                      "description": "Granular reply breakdown by classification"
                    }
                  },
                  "required": [
                    "step",
                    "sent",
                    "delivered",
                    "opened",
                    "clicked",
                    "bounced",
                    "unsubscribed",
                    "repliesPositive",
                    "repliesNegative",
                    "repliesNeutral",
                    "repliesAutoReply",
                    "repliesDetail"
                  ]
                },
                "description": "Per-step breakdown (instantly broadcast only; empty/omitted for postmark transactional)"
              }
            },
            "required": [
              "sent",
              "delivered",
              "opened",
              "clicked",
              "bounced",
              "unsubscribed"
            ]
          }
        },
        "required": [
          "recipientStats",
          "emailStats"
        ]
      },
      "StatsGroupedResponse": {
        "type": "object",
        "properties": {
          "groups": {
            "type": "array",
            "items": {
              "type": "object",
              "properties": {
                "key": {
                  "type": "string",
                  "description": "Group key. For groupBy=day this is YYYY-MM-DD in the requested timezone."
                },
                "recipientStats": {
                  "type": "object",
                  "properties": {
                    "contacted": {
                      "type": "number",
                      "description": "Leads added to a campaign / send attempted"
                    },
                    "sent": {
                      "type": "number",
                      "description": "Leads with at least 1 email sent (COUNT DISTINCT lead)"
                    },
                    "delivered": {
                      "type": "number",
                      "description": "Leads delivered (sent - bounced)"
                    },
                    "opened": {
                      "type": "number",
                      "description": "Leads who opened at least 1 email"
                    },
                    "bounced": {
                      "type": "number",
                      "description": "Leads with at least 1 bounce"
                    },
                    "clicked": {
                      "type": "number",
                      "description": "Leads who clicked at least 1 link"
                    },
                    "unsubscribed": {
                      "type": "number",
                      "description": "Leads who unsubscribed"
                    },
                    "notSending": {
                      "type": "number",
                      "description": "Leads in campaigns currently flagged with Instantly's not_sending_status diagnostic. Optional in contract v1; will be required in v2 after postmark padding ships."
                    },
                    "cancelled": {
                      "type": "number",
                      "description": "Leads whose campaign was cancelled by the retry-stuck job (delivery_status='cancelled'). Optional in contract v1; will be required in v2 after postmark padding ships."
                    },
                    "repliesPositive": {
                      "type": "number",
                      "description": "interested + meetingBooked + closed"
                    },
                    "repliesNegative": {
                      "type": "number",
                      "description": "notInterested + wrongPerson + unsubscribe"
                    },
                    "repliesNeutral": {
                      "type": "number",
                      "description": "neutral (lead_neutral events only)"
                    },
                    "repliesAutoReply": {
                      "type": "number",
                      "description": "autoReply + outOfOffice"
                    },
                    "repliesDetail": {
                      "type": "object",
                      "properties": {
                        "interested": {
                          "type": "number",
                          "description": "lead_interested events"
                        },
                        "meetingBooked": {
                          "type": "number",
                          "description": "lead_meeting_booked events"
                        },
                        "closed": {
                          "type": "number",
                          "description": "lead_closed events"
                        },
                        "notInterested": {
                          "type": "number",
                          "description": "lead_not_interested events"
                        },
                        "wrongPerson": {
                          "type": "number",
                          "description": "lead_wrong_person events"
                        },
                        "unsubscribe": {
                          "type": "number",
                          "description": "lead_unsubscribed events"
                        },
                        "neutral": {
                          "type": "number",
                          "description": "lead_neutral events"
                        },
                        "autoReply": {
                          "type": "number",
                          "description": "auto_reply_received events"
                        },
                        "outOfOffice": {
                          "type": "number",
                          "description": "lead_out_of_office events"
                        }
                      },
                      "required": [
                        "interested",
                        "meetingBooked",
                        "closed",
                        "notInterested",
                        "wrongPerson",
                        "unsubscribe",
                        "neutral",
                        "autoReply",
                        "outOfOffice"
                      ],
                      "description": "Granular reply breakdown by classification"
                    }
                  },
                  "required": [
                    "contacted",
                    "sent",
                    "delivered",
                    "opened",
                    "bounced",
                    "clicked",
                    "unsubscribed",
                    "repliesPositive",
                    "repliesNegative",
                    "repliesNeutral",
                    "repliesAutoReply",
                    "repliesDetail"
                  ]
                },
                "emailStats": {
                  "type": "object",
                  "properties": {
                    "sent": {
                      "type": "number",
                      "description": "Total emails sent (COUNT *, all steps)"
                    },
                    "delivered": {
                      "type": "number",
                      "description": "Total emails delivered (sent - bounced)"
                    },
                    "opened": {
                      "type": "number",
                      "description": "Unique emails opened at least once (COUNT DISTINCT)"
                    },
                    "clicked": {
                      "type": "number",
                      "description": "Unique emails with at least 1 click (COUNT DISTINCT)"
                    },
                    "bounced": {
                      "type": "number",
                      "description": "Total emails bounced"
                    },
                    "unsubscribed": {
                      "type": "number",
                      "description": "Total unsubscribe events"
                    },
                    "stepStats": {
                      "type": "array",
                      "items": {
                        "type": "object",
                        "properties": {
                          "step": {
                            "type": "number",
                            "description": "Step number (1-based)"
                          },
                          "sent": {
                            "type": "number",
                            "description": "Emails sent at this step"
                          },
                          "delivered": {
                            "type": "number",
                            "description": "Delivered at this step (sent - bounced)"
                          },
                          "opened": {
                            "type": "number",
                            "description": "Emails opened at this step"
                          },
                          "clicked": {
                            "type": "number",
                            "description": "Link clicks at this step"
                          },
                          "bounced": {
                            "type": "number",
                            "description": "Emails bounced at this step"
                          },
                          "unsubscribed": {
                            "type": "number",
                            "description": "Unsubscribes at this step"
                          },
                          "repliesPositive": {
                            "type": "number",
                            "description": "interested + meetingBooked + closed"
                          },
                          "repliesNegative": {
                            "type": "number",
                            "description": "notInterested + wrongPerson + unsubscribe"
                          },
                          "repliesNeutral": {
                            "type": "number",
                            "description": "neutral (lead_neutral events only)"
                          },
                          "repliesAutoReply": {
                            "type": "number",
                            "description": "autoReply + outOfOffice"
                          },
                          "repliesDetail": {
                            "type": "object",
                            "properties": {
                              "interested": {
                                "type": "number",
                                "description": "lead_interested events"
                              },
                              "meetingBooked": {
                                "type": "number",
                                "description": "lead_meeting_booked events"
                              },
                              "closed": {
                                "type": "number",
                                "description": "lead_closed events"
                              },
                              "notInterested": {
                                "type": "number",
                                "description": "lead_not_interested events"
                              },
                              "wrongPerson": {
                                "type": "number",
                                "description": "lead_wrong_person events"
                              },
                              "unsubscribe": {
                                "type": "number",
                                "description": "lead_unsubscribed events"
                              },
                              "neutral": {
                                "type": "number",
                                "description": "lead_neutral events"
                              },
                              "autoReply": {
                                "type": "number",
                                "description": "auto_reply_received events"
                              },
                              "outOfOffice": {
                                "type": "number",
                                "description": "lead_out_of_office events"
                              }
                            },
                            "required": [
                              "interested",
                              "meetingBooked",
                              "closed",
                              "notInterested",
                              "wrongPerson",
                              "unsubscribe",
                              "neutral",
                              "autoReply",
                              "outOfOffice"
                            ],
                            "description": "Granular reply breakdown by classification"
                          }
                        },
                        "required": [
                          "step",
                          "sent",
                          "delivered",
                          "opened",
                          "clicked",
                          "bounced",
                          "unsubscribed",
                          "repliesPositive",
                          "repliesNegative",
                          "repliesNeutral",
                          "repliesAutoReply",
                          "repliesDetail"
                        ]
                      },
                      "description": "Per-step breakdown (instantly broadcast only; empty/omitted for postmark transactional)"
                    }
                  },
                  "required": [
                    "sent",
                    "delivered",
                    "opened",
                    "clicked",
                    "bounced",
                    "unsubscribed"
                  ]
                }
              },
              "required": [
                "key",
                "recipientStats",
                "emailStats"
              ]
            }
          }
        },
        "required": [
          "groups"
        ]
      },
      "EngagementLatencyResponse": {
        "type": "object",
        "properties": {
          "workflowSlugs": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Workflow slugs included in this aggregate."
          },
          "timeToFirstLinkClick": {
            "$ref": "#/components/schemas/EngagementLatencyMetric"
          },
          "timeToFirstPositiveReply": {
            "$ref": "#/components/schemas/EngagementLatencyMetric"
          }
        },
        "required": [
          "workflowSlugs",
          "timeToFirstLinkClick",
          "timeToFirstPositiveReply"
        ]
      },
      "EngagementLatencyMetric": {
        "type": "object",
        "properties": {
          "averageMs": {
            "type": "number",
            "nullable": true,
            "description": "Average elapsed time in milliseconds. Null when sampleSize is 0."
          },
          "medianMs": {
            "type": "number",
            "nullable": true,
            "description": "Median elapsed time in milliseconds. Null when sampleSize is 0."
          },
          "sampleSize": {
            "type": "integer",
            "description": "Number of recipients included in the aggregate."
          }
        },
        "required": [
          "averageMs",
          "medianMs",
          "sampleSize"
        ]
      },
      "EngagementLatencyGroupedResponse": {
        "type": "object",
        "properties": {
          "groups": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/EngagementLatencyGroupedEntry"
            }
          }
        },
        "required": [
          "groups"
        ]
      },
      "EngagementLatencyGroupedEntry": {
        "type": "object",
        "properties": {
          "key": {
            "type": "string",
            "description": "Caller-owned public group key, for example a workflow dynasty slug."
          },
          "workflowSlugs": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "timeToFirstLinkClick": {
            "$ref": "#/components/schemas/EngagementLatencyMetric"
          },
          "timeToFirstPositiveReply": {
            "$ref": "#/components/schemas/EngagementLatencyMetric"
          }
        },
        "required": [
          "key",
          "workflowSlugs",
          "timeToFirstLinkClick",
          "timeToFirstPositiveReply"
        ]
      },
      "EngagementLatencyGroupedRequest": {
        "type": "object",
        "properties": {
          "groups": {
            "type": "object",
            "additionalProperties": {
              "type": "object",
              "properties": {
                "workflowSlugs": {
                  "type": "array",
                  "items": {
                    "type": "string",
                    "minLength": 1
                  },
                  "minItems": 1,
                  "description": "Workflow slugs included in this public-safe group."
                }
              },
              "required": [
                "workflowSlugs"
              ]
            }
          }
        },
        "required": [
          "groups"
        ]
      },
      "GroupedStatsResponse": {
        "type": "object",
        "properties": {
          "groups": {
            "type": "array",
            "items": {
              "type": "object",
              "properties": {
                "key": {
                  "type": "string",
                  "description": "Group key from the request"
                },
                "recipientStats": {
                  "type": "object",
                  "properties": {
                    "contacted": {
                      "type": "number",
                      "description": "Leads added to a campaign / send attempted"
                    },
                    "sent": {
                      "type": "number",
                      "description": "Leads with at least 1 email sent (COUNT DISTINCT lead)"
                    },
                    "delivered": {
                      "type": "number",
                      "description": "Leads delivered (sent - bounced)"
                    },
                    "opened": {
                      "type": "number",
                      "description": "Leads who opened at least 1 email"
                    },
                    "bounced": {
                      "type": "number",
                      "description": "Leads with at least 1 bounce"
                    },
                    "clicked": {
                      "type": "number",
                      "description": "Leads who clicked at least 1 link"
                    },
                    "unsubscribed": {
                      "type": "number",
                      "description": "Leads who unsubscribed"
                    },
                    "notSending": {
                      "type": "number",
                      "description": "Leads in campaigns currently flagged with Instantly's not_sending_status diagnostic. Optional in contract v1; will be required in v2 after postmark padding ships."
                    },
                    "cancelled": {
                      "type": "number",
                      "description": "Leads whose campaign was cancelled by the retry-stuck job (delivery_status='cancelled'). Optional in contract v1; will be required in v2 after postmark padding ships."
                    },
                    "repliesPositive": {
                      "type": "number",
                      "description": "interested + meetingBooked + closed"
                    },
                    "repliesNegative": {
                      "type": "number",
                      "description": "notInterested + wrongPerson + unsubscribe"
                    },
                    "repliesNeutral": {
                      "type": "number",
                      "description": "neutral (lead_neutral events only)"
                    },
                    "repliesAutoReply": {
                      "type": "number",
                      "description": "autoReply + outOfOffice"
                    },
                    "repliesDetail": {
                      "type": "object",
                      "properties": {
                        "interested": {
                          "type": "number",
                          "description": "lead_interested events"
                        },
                        "meetingBooked": {
                          "type": "number",
                          "description": "lead_meeting_booked events"
                        },
                        "closed": {
                          "type": "number",
                          "description": "lead_closed events"
                        },
                        "notInterested": {
                          "type": "number",
                          "description": "lead_not_interested events"
                        },
                        "wrongPerson": {
                          "type": "number",
                          "description": "lead_wrong_person events"
                        },
                        "unsubscribe": {
                          "type": "number",
                          "description": "lead_unsubscribed events"
                        },
                        "neutral": {
                          "type": "number",
                          "description": "lead_neutral events"
                        },
                        "autoReply": {
                          "type": "number",
                          "description": "auto_reply_received events"
                        },
                        "outOfOffice": {
                          "type": "number",
                          "description": "lead_out_of_office events"
                        }
                      },
                      "required": [
                        "interested",
                        "meetingBooked",
                        "closed",
                        "notInterested",
                        "wrongPerson",
                        "unsubscribe",
                        "neutral",
                        "autoReply",
                        "outOfOffice"
                      ],
                      "description": "Granular reply breakdown by classification"
                    }
                  },
                  "required": [
                    "contacted",
                    "sent",
                    "delivered",
                    "opened",
                    "bounced",
                    "clicked",
                    "unsubscribed",
                    "repliesPositive",
                    "repliesNegative",
                    "repliesNeutral",
                    "repliesAutoReply",
                    "repliesDetail"
                  ]
                },
                "emailStats": {
                  "type": "object",
                  "properties": {
                    "sent": {
                      "type": "number",
                      "description": "Total emails sent (COUNT *, all steps)"
                    },
                    "delivered": {
                      "type": "number",
                      "description": "Total emails delivered (sent - bounced)"
                    },
                    "opened": {
                      "type": "number",
                      "description": "Unique emails opened at least once (COUNT DISTINCT)"
                    },
                    "clicked": {
                      "type": "number",
                      "description": "Unique emails with at least 1 click (COUNT DISTINCT)"
                    },
                    "bounced": {
                      "type": "number",
                      "description": "Total emails bounced"
                    },
                    "unsubscribed": {
                      "type": "number",
                      "description": "Total unsubscribe events"
                    },
                    "stepStats": {
                      "type": "array",
                      "items": {
                        "type": "object",
                        "properties": {
                          "step": {
                            "type": "number",
                            "description": "Step number (1-based)"
                          },
                          "sent": {
                            "type": "number",
                            "description": "Emails sent at this step"
                          },
                          "delivered": {
                            "type": "number",
                            "description": "Delivered at this step (sent - bounced)"
                          },
                          "opened": {
                            "type": "number",
                            "description": "Emails opened at this step"
                          },
                          "clicked": {
                            "type": "number",
                            "description": "Link clicks at this step"
                          },
                          "bounced": {
                            "type": "number",
                            "description": "Emails bounced at this step"
                          },
                          "unsubscribed": {
                            "type": "number",
                            "description": "Unsubscribes at this step"
                          },
                          "repliesPositive": {
                            "type": "number",
                            "description": "interested + meetingBooked + closed"
                          },
                          "repliesNegative": {
                            "type": "number",
                            "description": "notInterested + wrongPerson + unsubscribe"
                          },
                          "repliesNeutral": {
                            "type": "number",
                            "description": "neutral (lead_neutral events only)"
                          },
                          "repliesAutoReply": {
                            "type": "number",
                            "description": "autoReply + outOfOffice"
                          },
                          "repliesDetail": {
                            "type": "object",
                            "properties": {
                              "interested": {
                                "type": "number",
                                "description": "lead_interested events"
                              },
                              "meetingBooked": {
                                "type": "number",
                                "description": "lead_meeting_booked events"
                              },
                              "closed": {
                                "type": "number",
                                "description": "lead_closed events"
                              },
                              "notInterested": {
                                "type": "number",
                                "description": "lead_not_interested events"
                              },
                              "wrongPerson": {
                                "type": "number",
                                "description": "lead_wrong_person events"
                              },
                              "unsubscribe": {
                                "type": "number",
                                "description": "lead_unsubscribed events"
                              },
                              "neutral": {
                                "type": "number",
                                "description": "lead_neutral events"
                              },
                              "autoReply": {
                                "type": "number",
                                "description": "auto_reply_received events"
                              },
                              "outOfOffice": {
                                "type": "number",
                                "description": "lead_out_of_office events"
                              }
                            },
                            "required": [
                              "interested",
                              "meetingBooked",
                              "closed",
                              "notInterested",
                              "wrongPerson",
                              "unsubscribe",
                              "neutral",
                              "autoReply",
                              "outOfOffice"
                            ],
                            "description": "Granular reply breakdown by classification"
                          }
                        },
                        "required": [
                          "step",
                          "sent",
                          "delivered",
                          "opened",
                          "clicked",
                          "bounced",
                          "unsubscribed",
                          "repliesPositive",
                          "repliesNegative",
                          "repliesNeutral",
                          "repliesAutoReply",
                          "repliesDetail"
                        ]
                      },
                      "description": "Per-step breakdown (instantly broadcast only; empty/omitted for postmark transactional)"
                    }
                  },
                  "required": [
                    "sent",
                    "delivered",
                    "opened",
                    "clicked",
                    "bounced",
                    "unsubscribed"
                  ]
                }
              },
              "required": [
                "key",
                "recipientStats",
                "emailStats"
              ]
            }
          }
        },
        "required": [
          "groups"
        ]
      },
      "GroupedStatsRequest": {
        "type": "object",
        "properties": {
          "groups": {
            "type": "object",
            "additionalProperties": {
              "type": "object",
              "properties": {
                "runIds": {
                  "type": "array",
                  "items": {
                    "type": "string"
                  },
                  "minItems": 1
                }
              },
              "required": [
                "runIds"
              ]
            }
          }
        },
        "required": [
          "groups"
        ]
      },
      "SyncResponse": {
        "type": "object",
        "properties": {
          "success": {
            "type": "boolean"
          },
          "synced": {
            "type": "number"
          }
        },
        "required": [
          "success",
          "synced"
        ]
      },
      "TransferBrandResponse": {
        "type": "object",
        "properties": {
          "updatedTables": {
            "type": "array",
            "items": {
              "type": "object",
              "properties": {
                "tableName": {
                  "type": "string"
                },
                "count": {
                  "type": "number"
                }
              },
              "required": [
                "tableName",
                "count"
              ]
            }
          }
        },
        "required": [
          "updatedTables"
        ]
      },
      "TransferBrandRequest": {
        "type": "object",
        "properties": {
          "sourceBrandId": {
            "type": "string",
            "description": "Brand UUID to transfer from the source org"
          },
          "sourceOrgId": {
            "type": "string",
            "description": "Current org UUID that owns the brand"
          },
          "targetOrgId": {
            "type": "string",
            "description": "Destination org UUID"
          },
          "targetBrandId": {
            "type": "string",
            "description": "Brand UUID in the target org — when present, rewrites brand_id references to this value"
          }
        },
        "required": [
          "sourceBrandId",
          "sourceOrgId",
          "targetOrgId"
        ]
      },
      "WarmupResponse": {
        "type": "object",
        "properties": {
          "success": {
            "type": "boolean"
          },
          "email": {
            "type": "string"
          },
          "warmupEnabled": {
            "type": "boolean"
          }
        },
        "required": [
          "success",
          "email",
          "warmupEnabled"
        ]
      },
      "WarmupRequest": {
        "type": "object",
        "properties": {
          "enabled": {
            "type": "boolean"
          }
        },
        "required": [
          "enabled"
        ]
      },
      "ReplyToLeadResponse": {
        "type": "object",
        "properties": {
          "success": {
            "type": "boolean",
            "enum": [
              true
            ]
          },
          "reply": {
            "$ref": "#/components/schemas/ReplyToLeadResult"
          }
        },
        "required": [
          "success",
          "reply"
        ]
      },
      "ReplyToLeadResult": {
        "type": "object",
        "properties": {
          "transport": {
            "type": "string",
            "enum": [
              "instantly",
              "smtp"
            ],
            "description": "Which pipe carried the reply — the one that carried the outreach"
          },
          "instantlyCampaignId": {
            "type": "string"
          },
          "leadEmail": {
            "type": "string"
          },
          "accountEmail": {
            "type": "string",
            "description": "The mailbox that answered. Resolved by this service, never supplied by the caller."
          },
          "from": {
            "type": "string",
            "description": "The From header as the prospect sees it, persona included"
          },
          "subject": {
            "type": "string",
            "description": "The conversation's own subject under `Re:`"
          },
          "messageId": {
            "type": "string",
            "description": "Instantly's email id, or the RFC 5322 Message-Id we sent"
          },
          "inReplyTo": {
            "type": "string",
            "description": "The prospect message this reply threads onto"
          }
        },
        "required": [
          "transport",
          "instantlyCampaignId",
          "leadEmail",
          "accountEmail",
          "from",
          "subject",
          "messageId",
          "inReplyTo"
        ]
      },
      "ReplyToLeadError": {
        "type": "object",
        "properties": {
          "error": {
            "type": "string"
          },
          "code": {
            "type": "string",
            "enum": [
              "campaign_not_found",
              "no_reply_to_thread",
              "sending_account_unresolved",
              "mailbox_credential_unavailable",
              "reply_dispatch_failed"
            ],
            "description": "campaign_not_found: no campaign in this org for the given email. no_reply_to_thread: the lead never wrote back, so there is nothing to thread onto. sending_account_unresolved: we cannot tell which mailbox contacted them. mailbox_credential_unavailable: the mailbox is on our own sender and we hold no credential for it. reply_dispatch_failed: the transport refused the send."
          }
        },
        "required": [
          "error",
          "code"
        ]
      },
      "ReplyToLeadBody": {
        "type": "object",
        "properties": {
          "campaign_id": {
            "type": "string",
            "minLength": 1,
            "description": "Logical campaign id — the same key manual qualifications and opt-outs use"
          },
          "email": {
            "type": "string",
            "format": "email",
            "description": "The lead who replied"
          },
          "body_html": {
            "type": "string",
            "minLength": 1,
            "description": "The answer, HTML. Signed by this service with the sending account's persona — do NOT include a signature."
          }
        },
        "required": [
          "campaign_id",
          "email",
          "body_html"
        ],
        "example": {
          "campaign_id": "c1a2b3c4-0000-0000-0000-000000000001",
          "email": "alice@media.com",
          "body_html": "<p>Great — how does Thursday 3pm look?</p>"
        }
      },
      "LeadConversationResponse": {
        "type": "object",
        "properties": {
          "success": {
            "type": "boolean",
            "enum": [
              true
            ]
          },
          "conversation": {
            "$ref": "#/components/schemas/LeadConversation"
          }
        },
        "required": [
          "success",
          "conversation"
        ]
      },
      "LeadConversation": {
        "type": "object",
        "properties": {
          "campaignId": {
            "type": "string",
            "description": "The campaign id asked for"
          },
          "campaignIds": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Every stored campaign row of this campaign holding this lead, oldest first. campaign-service keeps an ancestor row per workflow change, so one campaign is routinely many rows and a prospect emailed over months sits in several — the exchange spans all of them."
          },
          "instantlyCampaignId": {
            "type": "string",
            "description": "The asked row's sequence id; each message carries the row it came from"
          },
          "leadEmail": {
            "type": "string",
            "description": "The stored casing, which may differ from the one asked for"
          },
          "accountEmail": {
            "type": "string",
            "nullable": true,
            "description": "The asked row's mailbox; null on a row predating the account persist"
          },
          "transport": {
            "type": "string",
            "enum": [
              "instantly",
              "smtp"
            ],
            "description": "The asked row's pipe — the caller does not need to know this to ask"
          },
          "source": {
            "type": "string",
            "enum": [
              "mirror",
              "self_send",
              "provider"
            ],
            "description": "Where the ASKED row's messages were read from; `sequences` says it per row."
          },
          "messageCount": {
            "type": "integer"
          },
          "messages": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/ConversationMessage"
            },
            "description": "Oldest first, across every contributing row. Empty when nothing has been exchanged."
          },
          "sequences": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/ConversationSequence"
            },
            "description": "What each contributing row carried, oldest first"
          }
        },
        "required": [
          "campaignId",
          "campaignIds",
          "instantlyCampaignId",
          "leadEmail",
          "accountEmail",
          "transport",
          "source",
          "messageCount",
          "messages",
          "sequences"
        ]
      },
      "ConversationMessage": {
        "type": "object",
        "properties": {
          "direction": {
            "type": "string",
            "enum": [
              "inbound",
              "outbound"
            ],
            "description": "inbound: the prospect wrote it. outbound: we did."
          },
          "from": {
            "type": "string"
          },
          "to": {
            "type": "string"
          },
          "at": {
            "type": "string",
            "description": "ISO 8601 UTC. Empty only when the source carried no timestamp."
          },
          "subject": {
            "type": "string"
          },
          "text": {
            "type": "string",
            "description": "The message as readable TEXT — markup stripped, never HTML"
          },
          "campaignId": {
            "type": "string",
            "description": "The stored campaign row this message was exchanged under"
          },
          "instantlyCampaignId": {
            "type": "string",
            "description": "That row's sequence id"
          }
        },
        "required": [
          "direction",
          "from",
          "to",
          "at",
          "subject",
          "text",
          "campaignId",
          "instantlyCampaignId"
        ]
      },
      "ConversationSequence": {
        "type": "object",
        "properties": {
          "campaignId": {
            "type": "string"
          },
          "instantlyCampaignId": {
            "type": "string"
          },
          "accountEmail": {
            "type": "string",
            "nullable": true
          },
          "transport": {
            "type": "string",
            "enum": [
              "instantly",
              "smtp"
            ]
          },
          "source": {
            "type": "string",
            "enum": [
              "mirror",
              "self_send",
              "provider"
            ],
            "description": "Where the messages were read from. mirror: our own bronze copy of the Instantly Unibox — the normal case, and the one that survives the Instantly plan being cancelled. self_send: the sequence we dispatched ourselves. provider: read live from Instantly because our mirror held nothing for a sequence that did exchange mail."
          },
          "messageCount": {
            "type": "integer"
          }
        },
        "required": [
          "campaignId",
          "instantlyCampaignId",
          "accountEmail",
          "transport",
          "source",
          "messageCount"
        ]
      },
      "LeadConversationError": {
        "type": "object",
        "properties": {
          "error": {
            "type": "string"
          },
          "code": {
            "type": "string",
            "enum": [
              "campaign_not_found",
              "thread_unavailable",
              "campaign_identity_unavailable",
              "too_many_sequences"
            ],
            "description": "campaign_not_found: this org holds no such exchange for that email — distinct from an exchange that exists and is empty, which is a 200 with messages: []. thread_unavailable: it exists but could not be read; returning it as empty would claim the prospect said nothing. campaign_identity_unavailable: campaign-service could not say which rows this campaign is made of, so only part of the exchange could be found. too_many_sequences: the lead sits in more rows of this campaign than the read will fan out to."
          }
        },
        "required": [
          "error",
          "code"
        ]
      },
      "EngagedLeadsResponse": {
        "type": "object",
        "properties": {
          "success": {
            "type": "boolean",
            "enum": [
              true
            ]
          },
          "count": {
            "type": "integer"
          },
          "leads": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/EngagedLead"
            },
            "description": "Most recently engaged first"
          }
        },
        "required": [
          "success",
          "count",
          "leads"
        ]
      },
      "EngagedLead": {
        "type": "object",
        "properties": {
          "campaignId": {
            "type": "string",
            "nullable": true,
            "description": "The caller's own campaign id. Null on a platform send."
          },
          "instantlyCampaignId": {
            "type": "string",
            "description": "This service's per-lead sequence id — present even when campaignId is null"
          },
          "leadEmail": {
            "type": "string"
          },
          "brandIds": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "engagedAt": {
            "type": "string",
            "description": "When this lead FIRST showed interest — the earlier of their first reply and their first click"
          },
          "replied": {
            "type": "boolean"
          },
          "clicked": {
            "type": "boolean"
          },
          "firstRepliedAt": {
            "type": "string",
            "nullable": true
          },
          "firstClickedAt": {
            "type": "string",
            "nullable": true
          },
          "replyClassification": {
            "type": "string",
            "nullable": true,
            "description": "positive | negative | neutral. Null when no reply is qualified."
          },
          "replyKind": {
            "type": "string",
            "nullable": true,
            "description": "The finer reading of the same statement. Null when none is on record."
          },
          "disqualified": {
            "type": "boolean",
            "description": "True only for a kind that is permanent about the PERSON (wrong person, changed job) — never for a 'not right now', which stays recyclable"
          }
        },
        "required": [
          "campaignId",
          "instantlyCampaignId",
          "leadEmail",
          "brandIds",
          "engagedAt",
          "replied",
          "clicked",
          "firstRepliedAt",
          "firstClickedAt",
          "replyClassification",
          "replyKind",
          "disqualified"
        ]
      },
      "ManualQualificationCreateResponse": {
        "type": "object",
        "properties": {
          "idempotent": {
            "type": "boolean",
            "description": "True if the latest existing row already matched the requested status — no new bronze row was inserted, no side effects fired"
          },
          "qualification": {
            "type": "object",
            "properties": {
              "id": {
                "type": "string"
              },
              "orgId": {
                "type": "string"
              },
              "campaignId": {
                "type": "string"
              },
              "instantlyCampaignId": {
                "type": "string"
              },
              "email": {
                "type": "string"
              },
              "status": {
                "type": "string",
                "enum": [
                  "lead_interested",
                  "lead_referral",
                  "lead_info_requested",
                  "lead_meeting_requested",
                  "lead_not_interested",
                  "lead_wrong_person",
                  "lead_changed_job",
                  "lead_neutral",
                  "lead_out_of_office",
                  "auto_reply_received",
                  "lead_meeting_booked",
                  "lead_closed"
                ],
                "description": "The raw statement a human made about a reply. Normally a reply kind; the two deal-progress values (lead_meeting_booked, lead_closed) are still ACCEPTED while the consoles migrate their pickers and resolve to lead_interested at write."
              },
              "replyKind": {
                "type": "string",
                "enum": [
                  "lead_interested",
                  "lead_referral",
                  "lead_info_requested",
                  "lead_meeting_requested",
                  "lead_not_interested",
                  "lead_wrong_person",
                  "lead_changed_job",
                  "lead_neutral",
                  "lead_out_of_office",
                  "auto_reply_received"
                ],
                "description": "The reply kind this statement resolved to at write time. Always a value of the current vocabulary, including for a statement made with a legacy deal-progress value."
              },
              "qualifiedBy": {
                "type": "string"
              },
              "notes": {
                "type": "string",
                "nullable": true
              },
              "qualifiedAt": {
                "type": "string",
                "description": "ISO 8601 timestamp"
              },
              "withdrawnAt": {
                "type": "string",
                "nullable": true,
                "description": "ISO 8601 timestamp of the withdrawal, or null while the statement still STANDS. Non-null means a human took this statement back: it is kept for audit and must never be rendered as the lead's current reply kind."
              },
              "withdrawnBy": {
                "type": "string",
                "nullable": true,
                "description": "Who withdrew the statement, or null while it still stands."
              }
            },
            "required": [
              "id",
              "orgId",
              "campaignId",
              "instantlyCampaignId",
              "email",
              "status",
              "replyKind",
              "qualifiedBy",
              "notes",
              "qualifiedAt",
              "withdrawnAt",
              "withdrawnBy"
            ]
          }
        },
        "required": [
          "idempotent",
          "qualification"
        ]
      },
      "ManualQualificationCreateBody": {
        "type": "object",
        "properties": {
          "campaign_id": {
            "type": "string",
            "minLength": 1,
            "description": "Logical campaign id (groups sub-campaigns for the same workflow run)"
          },
          "email": {
            "type": "string",
            "format": "email",
            "description": "Lead email address"
          },
          "status": {
            "type": "string",
            "enum": [
              "lead_interested",
              "lead_referral",
              "lead_info_requested",
              "lead_meeting_requested",
              "lead_not_interested",
              "lead_wrong_person",
              "lead_changed_job",
              "lead_neutral",
              "lead_out_of_office",
              "auto_reply_received",
              "lead_meeting_booked",
              "lead_closed"
            ],
            "description": "The raw statement a human made about a reply. Normally a reply kind; the two deal-progress values (lead_meeting_booked, lead_closed) are still ACCEPTED while the consoles migrate their pickers and resolve to lead_interested at write."
          },
          "notes": {
            "type": "string",
            "maxLength": 2000,
            "description": "Optional free-text human note for audit"
          }
        },
        "required": [
          "campaign_id",
          "email",
          "status"
        ],
        "example": {
          "campaign_id": "c1a2b3c4-0000-0000-0000-000000000001",
          "email": "alice@media.com",
          "status": "lead_interested",
          "notes": "Reply received on Gmail — Instantly missed it"
        }
      },
      "ManualQualificationWithdrawResponse": {
        "type": "object",
        "properties": {
          "qualification": {
            "type": "object",
            "properties": {
              "id": {
                "type": "string"
              },
              "orgId": {
                "type": "string"
              },
              "campaignId": {
                "type": "string"
              },
              "instantlyCampaignId": {
                "type": "string"
              },
              "email": {
                "type": "string"
              },
              "status": {
                "type": "string",
                "enum": [
                  "lead_interested",
                  "lead_referral",
                  "lead_info_requested",
                  "lead_meeting_requested",
                  "lead_not_interested",
                  "lead_wrong_person",
                  "lead_changed_job",
                  "lead_neutral",
                  "lead_out_of_office",
                  "auto_reply_received",
                  "lead_meeting_booked",
                  "lead_closed"
                ],
                "description": "The raw statement a human made about a reply. Normally a reply kind; the two deal-progress values (lead_meeting_booked, lead_closed) are still ACCEPTED while the consoles migrate their pickers and resolve to lead_interested at write."
              },
              "replyKind": {
                "type": "string",
                "enum": [
                  "lead_interested",
                  "lead_referral",
                  "lead_info_requested",
                  "lead_meeting_requested",
                  "lead_not_interested",
                  "lead_wrong_person",
                  "lead_changed_job",
                  "lead_neutral",
                  "lead_out_of_office",
                  "auto_reply_received"
                ],
                "description": "The reply kind this statement resolved to at write time. Always a value of the current vocabulary, including for a statement made with a legacy deal-progress value."
              },
              "qualifiedBy": {
                "type": "string"
              },
              "notes": {
                "type": "string",
                "nullable": true
              },
              "qualifiedAt": {
                "type": "string",
                "description": "ISO 8601 timestamp"
              },
              "withdrawnAt": {
                "type": "string",
                "nullable": true,
                "description": "ISO 8601 timestamp of the withdrawal, or null while the statement still STANDS. Non-null means a human took this statement back: it is kept for audit and must never be rendered as the lead's current reply kind."
              },
              "withdrawnBy": {
                "type": "string",
                "nullable": true,
                "description": "Who withdrew the statement, or null while it still stands."
              }
            },
            "required": [
              "id",
              "orgId",
              "campaignId",
              "instantlyCampaignId",
              "email",
              "status",
              "replyKind",
              "qualifiedBy",
              "notes",
              "qualifiedAt",
              "withdrawnAt",
              "withdrawnBy"
            ],
            "description": "The statement that was withdrawn, now carrying withdrawnAt / withdrawnBy."
          }
        },
        "required": [
          "qualification"
        ]
      },
      "ManualQualificationWithdrawError": {
        "type": "object",
        "properties": {
          "error": {
            "type": "string"
          },
          "code": {
            "type": "string",
            "enum": [
              "no_standing_qualification",
              "campaign_not_found"
            ],
            "description": "no_standing_qualification: nobody currently stands behind a reply kind for this pair (nothing was ever stated, or the statement is already withdrawn). campaign_not_found: no campaign in this org for the given email."
          }
        },
        "required": [
          "error",
          "code"
        ]
      },
      "ManualQualificationWithdrawBody": {
        "type": "object",
        "properties": {
          "campaign_id": {
            "type": "string",
            "minLength": 1,
            "description": "Logical campaign id (groups sub-campaigns for the same workflow run)"
          },
          "email": {
            "type": "string",
            "format": "email",
            "description": "Lead email address"
          },
          "notes": {
            "type": "string",
            "maxLength": 2000,
            "description": "Optional free-text human note for audit"
          }
        },
        "required": [
          "campaign_id",
          "email"
        ],
        "example": {
          "campaign_id": "c1a2b3c4-0000-0000-0000-000000000001",
          "email": "alice@media.com",
          "notes": "Picked the wrong kind by mistake"
        }
      },
      "ManualQualificationListResponse": {
        "type": "object",
        "properties": {
          "qualifications": {
            "type": "array",
            "items": {
              "type": "object",
              "properties": {
                "id": {
                  "type": "string"
                },
                "orgId": {
                  "type": "string"
                },
                "campaignId": {
                  "type": "string"
                },
                "instantlyCampaignId": {
                  "type": "string"
                },
                "email": {
                  "type": "string"
                },
                "status": {
                  "type": "string",
                  "enum": [
                    "lead_interested",
                    "lead_referral",
                    "lead_info_requested",
                    "lead_meeting_requested",
                    "lead_not_interested",
                    "lead_wrong_person",
                    "lead_changed_job",
                    "lead_neutral",
                    "lead_out_of_office",
                    "auto_reply_received",
                    "lead_meeting_booked",
                    "lead_closed"
                  ],
                  "description": "The raw statement a human made about a reply. Normally a reply kind; the two deal-progress values (lead_meeting_booked, lead_closed) are still ACCEPTED while the consoles migrate their pickers and resolve to lead_interested at write."
                },
                "replyKind": {
                  "type": "string",
                  "enum": [
                    "lead_interested",
                    "lead_referral",
                    "lead_info_requested",
                    "lead_meeting_requested",
                    "lead_not_interested",
                    "lead_wrong_person",
                    "lead_changed_job",
                    "lead_neutral",
                    "lead_out_of_office",
                    "auto_reply_received"
                  ],
                  "description": "The reply kind this statement resolved to at write time. Always a value of the current vocabulary, including for a statement made with a legacy deal-progress value."
                },
                "qualifiedBy": {
                  "type": "string"
                },
                "notes": {
                  "type": "string",
                  "nullable": true
                },
                "qualifiedAt": {
                  "type": "string",
                  "description": "ISO 8601 timestamp"
                },
                "withdrawnAt": {
                  "type": "string",
                  "nullable": true,
                  "description": "ISO 8601 timestamp of the withdrawal, or null while the statement still STANDS. Non-null means a human took this statement back: it is kept for audit and must never be rendered as the lead's current reply kind."
                },
                "withdrawnBy": {
                  "type": "string",
                  "nullable": true,
                  "description": "Who withdrew the statement, or null while it still stands."
                }
              },
              "required": [
                "id",
                "orgId",
                "campaignId",
                "instantlyCampaignId",
                "email",
                "status",
                "replyKind",
                "qualifiedBy",
                "notes",
                "qualifiedAt",
                "withdrawnAt",
                "withdrawnBy"
              ]
            }
          }
        },
        "required": [
          "qualifications"
        ]
      },
      "StatusResponse": {
        "type": "object",
        "properties": {
          "results": {
            "type": "array",
            "items": {
              "type": "object",
              "properties": {
                "email": {
                  "type": "string"
                },
                "byCampaign": {
                  "type": "object",
                  "nullable": true,
                  "additionalProperties": {
                    "type": "object",
                    "properties": {
                      "contacted": {
                        "type": "boolean",
                        "description": "Whether at least one sending exists in this scope"
                      },
                      "sent": {
                        "type": "boolean",
                        "description": "Whether an email was sent in this scope"
                      },
                      "delivered": {
                        "type": "boolean",
                        "description": "Whether an email was delivered in this scope (sent AND not bounced)"
                      },
                      "opened": {
                        "type": "boolean",
                        "description": "Whether the recipient opened any email in this scope"
                      },
                      "clicked": {
                        "type": "boolean",
                        "description": "Whether the recipient clicked any link in this scope"
                      },
                      "replied": {
                        "type": "boolean",
                        "description": "Whether the recipient replied in this scope (always false for postmark — no reply tracking)"
                      },
                      "replyClassification": {
                        "type": "string",
                        "nullable": true,
                        "enum": [
                          "positive",
                          "negative",
                          "neutral",
                          null
                        ],
                        "description": "Classification of most recent reply: positive / negative / neutral, or null if no reply (always null for postmark)"
                      },
                      "bounced": {
                        "type": "boolean",
                        "description": "Whether an email bounced in this scope"
                      },
                      "unsubscribed": {
                        "type": "boolean",
                        "description": "Whether the recipient unsubscribed in this scope"
                      },
                      "cancelled": {
                        "type": "boolean",
                        "description": "Whether the campaign was cancelled by the retry-stuck job. Optional in contract v1; required in v2 after postmark padding ships."
                      },
                      "lastDeliveredAt": {
                        "type": "string",
                        "nullable": true,
                        "description": "ISO 8601 timestamp of last delivery in this scope; null if none"
                      },
                      "firstContactedAt": {
                        "type": "string",
                        "nullable": true,
                        "description": "First-occurrence (MIN) ISO 8601 timestamp of a contacted event in this scope; null if it never happened in scope"
                      },
                      "firstSentAt": {
                        "type": "string",
                        "nullable": true,
                        "description": "First-occurrence (MIN) ISO 8601 timestamp of a sent event in this scope; null if it never happened in scope"
                      },
                      "firstDeliveredAt": {
                        "type": "string",
                        "nullable": true,
                        "description": "First-occurrence (MIN) ISO 8601 timestamp of a delivered event in this scope; null if it never happened in scope"
                      },
                      "firstOpenedAt": {
                        "type": "string",
                        "nullable": true,
                        "description": "First-occurrence (MIN) ISO 8601 timestamp of an opened event in this scope; null if it never happened in scope"
                      },
                      "firstClickedAt": {
                        "type": "string",
                        "nullable": true,
                        "description": "First-occurrence (MIN) ISO 8601 timestamp of a clicked event in this scope; null if it never happened in scope"
                      },
                      "firstRepliedAt": {
                        "type": "string",
                        "nullable": true,
                        "description": "First-occurrence (MIN) ISO 8601 timestamp of a replied event in this scope; null if it never happened in scope (always null for postmark — no reply tracking)"
                      },
                      "firstBouncedAt": {
                        "type": "string",
                        "nullable": true,
                        "description": "First-occurrence (MIN) ISO 8601 timestamp of a bounced event in this scope; null if it never happened in scope"
                      },
                      "firstUnsubscribedAt": {
                        "type": "string",
                        "nullable": true,
                        "description": "First-occurrence (MIN) ISO 8601 timestamp of an unsubscribed event in this scope; null if it never happened in scope"
                      },
                      "sentCount": {
                        "type": "integer",
                        "minimum": 0,
                        "description": "Number of emails actually sent to the recipient in this scope (send-event count, not a boolean); 0 when no send. Sequence position derives from it: 1 = initial, 2 = first follow-up, 3 = second follow-up. Optional in contract v1; brand scope = SUM across the brand's campaigns."
                      },
                      "replyKind": {
                        "type": "string",
                        "nullable": true,
                        "enum": [
                          "lead_interested",
                          "lead_referral",
                          "lead_info_requested",
                          "lead_meeting_requested",
                          "lead_not_interested",
                          "lead_wrong_person",
                          "lead_changed_job",
                          "lead_neutral",
                          "lead_out_of_office",
                          "auto_reply_received",
                          null
                        ],
                        "description": "WHICH reply this is — the finer reading of `replyClassification`, from the human statement when one exists and from the automatic classification otherwise. Null when no reply kind is on record."
                      },
                      "disqualified": {
                        "type": "boolean",
                        "description": "True iff this person is PERMANENTLY out: they are not the right contact (`lead_wrong_person`) or they have left the role (`lead_changed_job`). A prospect who simply declines today (`lead_not_interested`) is NOT disqualified — the lead stays recyclable. Strictly a function of `replyKind`, so the two fields never disagree; false whenever no kind is on record, because an absence is not a disqualification."
                      }
                    },
                    "required": [
                      "contacted",
                      "sent",
                      "delivered",
                      "opened",
                      "clicked",
                      "replied",
                      "replyClassification",
                      "bounced",
                      "unsubscribed",
                      "lastDeliveredAt",
                      "replyKind",
                      "disqualified"
                    ]
                  },
                  "description": "Per-campaign breakdown — present only when brandId is provided without campaignId"
                },
                "brand": {
                  "type": "object",
                  "nullable": true,
                  "properties": {
                    "contacted": {
                      "type": "boolean",
                      "description": "Whether at least one sending exists in this scope"
                    },
                    "sent": {
                      "type": "boolean",
                      "description": "Whether an email was sent in this scope"
                    },
                    "delivered": {
                      "type": "boolean",
                      "description": "Whether an email was delivered in this scope (sent AND not bounced)"
                    },
                    "opened": {
                      "type": "boolean",
                      "description": "Whether the recipient opened any email in this scope"
                    },
                    "clicked": {
                      "type": "boolean",
                      "description": "Whether the recipient clicked any link in this scope"
                    },
                    "replied": {
                      "type": "boolean",
                      "description": "Whether the recipient replied in this scope (always false for postmark — no reply tracking)"
                    },
                    "replyClassification": {
                      "type": "string",
                      "nullable": true,
                      "enum": [
                        "positive",
                        "negative",
                        "neutral",
                        null
                      ],
                      "description": "Classification of most recent reply: positive / negative / neutral, or null if no reply (always null for postmark)"
                    },
                    "bounced": {
                      "type": "boolean",
                      "description": "Whether an email bounced in this scope"
                    },
                    "unsubscribed": {
                      "type": "boolean",
                      "description": "Whether the recipient unsubscribed in this scope"
                    },
                    "cancelled": {
                      "type": "boolean",
                      "description": "Whether the campaign was cancelled by the retry-stuck job. Optional in contract v1; required in v2 after postmark padding ships."
                    },
                    "lastDeliveredAt": {
                      "type": "string",
                      "nullable": true,
                      "description": "ISO 8601 timestamp of last delivery in this scope; null if none"
                    },
                    "firstContactedAt": {
                      "type": "string",
                      "nullable": true,
                      "description": "First-occurrence (MIN) ISO 8601 timestamp of a contacted event in this scope; null if it never happened in scope"
                    },
                    "firstSentAt": {
                      "type": "string",
                      "nullable": true,
                      "description": "First-occurrence (MIN) ISO 8601 timestamp of a sent event in this scope; null if it never happened in scope"
                    },
                    "firstDeliveredAt": {
                      "type": "string",
                      "nullable": true,
                      "description": "First-occurrence (MIN) ISO 8601 timestamp of a delivered event in this scope; null if it never happened in scope"
                    },
                    "firstOpenedAt": {
                      "type": "string",
                      "nullable": true,
                      "description": "First-occurrence (MIN) ISO 8601 timestamp of an opened event in this scope; null if it never happened in scope"
                    },
                    "firstClickedAt": {
                      "type": "string",
                      "nullable": true,
                      "description": "First-occurrence (MIN) ISO 8601 timestamp of a clicked event in this scope; null if it never happened in scope"
                    },
                    "firstRepliedAt": {
                      "type": "string",
                      "nullable": true,
                      "description": "First-occurrence (MIN) ISO 8601 timestamp of a replied event in this scope; null if it never happened in scope (always null for postmark — no reply tracking)"
                    },
                    "firstBouncedAt": {
                      "type": "string",
                      "nullable": true,
                      "description": "First-occurrence (MIN) ISO 8601 timestamp of a bounced event in this scope; null if it never happened in scope"
                    },
                    "firstUnsubscribedAt": {
                      "type": "string",
                      "nullable": true,
                      "description": "First-occurrence (MIN) ISO 8601 timestamp of an unsubscribed event in this scope; null if it never happened in scope"
                    },
                    "sentCount": {
                      "type": "integer",
                      "minimum": 0,
                      "description": "Number of emails actually sent to the recipient in this scope (send-event count, not a boolean); 0 when no send. Sequence position derives from it: 1 = initial, 2 = first follow-up, 3 = second follow-up. Optional in contract v1; brand scope = SUM across the brand's campaigns."
                    },
                    "replyKind": {
                      "type": "string",
                      "nullable": true,
                      "enum": [
                        "lead_interested",
                        "lead_referral",
                        "lead_info_requested",
                        "lead_meeting_requested",
                        "lead_not_interested",
                        "lead_wrong_person",
                        "lead_changed_job",
                        "lead_neutral",
                        "lead_out_of_office",
                        "auto_reply_received",
                        null
                      ],
                      "description": "WHICH reply this is — the finer reading of `replyClassification`, from the human statement when one exists and from the automatic classification otherwise. Null when no reply kind is on record."
                    },
                    "disqualified": {
                      "type": "boolean",
                      "description": "True iff this person is PERMANENTLY out: they are not the right contact (`lead_wrong_person`) or they have left the role (`lead_changed_job`). A prospect who simply declines today (`lead_not_interested`) is NOT disqualified — the lead stays recyclable. Strictly a function of `replyKind`, so the two fields never disagree; false whenever no kind is on record, because an absence is not a disqualification."
                    }
                  },
                  "required": [
                    "contacted",
                    "sent",
                    "delivered",
                    "opened",
                    "clicked",
                    "replied",
                    "replyClassification",
                    "bounced",
                    "unsubscribed",
                    "lastDeliveredAt",
                    "replyKind",
                    "disqualified"
                  ],
                  "description": "Aggregated brand status (most advanced across campaigns) — present only when brandId is provided without campaignId"
                },
                "campaign": {
                  "type": "object",
                  "nullable": true,
                  "properties": {
                    "contacted": {
                      "type": "boolean",
                      "description": "Whether at least one sending exists in this scope"
                    },
                    "sent": {
                      "type": "boolean",
                      "description": "Whether an email was sent in this scope"
                    },
                    "delivered": {
                      "type": "boolean",
                      "description": "Whether an email was delivered in this scope (sent AND not bounced)"
                    },
                    "opened": {
                      "type": "boolean",
                      "description": "Whether the recipient opened any email in this scope"
                    },
                    "clicked": {
                      "type": "boolean",
                      "description": "Whether the recipient clicked any link in this scope"
                    },
                    "replied": {
                      "type": "boolean",
                      "description": "Whether the recipient replied in this scope (always false for postmark — no reply tracking)"
                    },
                    "replyClassification": {
                      "type": "string",
                      "nullable": true,
                      "enum": [
                        "positive",
                        "negative",
                        "neutral",
                        null
                      ],
                      "description": "Classification of most recent reply: positive / negative / neutral, or null if no reply (always null for postmark)"
                    },
                    "bounced": {
                      "type": "boolean",
                      "description": "Whether an email bounced in this scope"
                    },
                    "unsubscribed": {
                      "type": "boolean",
                      "description": "Whether the recipient unsubscribed in this scope"
                    },
                    "cancelled": {
                      "type": "boolean",
                      "description": "Whether the campaign was cancelled by the retry-stuck job. Optional in contract v1; required in v2 after postmark padding ships."
                    },
                    "lastDeliveredAt": {
                      "type": "string",
                      "nullable": true,
                      "description": "ISO 8601 timestamp of last delivery in this scope; null if none"
                    },
                    "firstContactedAt": {
                      "type": "string",
                      "nullable": true,
                      "description": "First-occurrence (MIN) ISO 8601 timestamp of a contacted event in this scope; null if it never happened in scope"
                    },
                    "firstSentAt": {
                      "type": "string",
                      "nullable": true,
                      "description": "First-occurrence (MIN) ISO 8601 timestamp of a sent event in this scope; null if it never happened in scope"
                    },
                    "firstDeliveredAt": {
                      "type": "string",
                      "nullable": true,
                      "description": "First-occurrence (MIN) ISO 8601 timestamp of a delivered event in this scope; null if it never happened in scope"
                    },
                    "firstOpenedAt": {
                      "type": "string",
                      "nullable": true,
                      "description": "First-occurrence (MIN) ISO 8601 timestamp of an opened event in this scope; null if it never happened in scope"
                    },
                    "firstClickedAt": {
                      "type": "string",
                      "nullable": true,
                      "description": "First-occurrence (MIN) ISO 8601 timestamp of a clicked event in this scope; null if it never happened in scope"
                    },
                    "firstRepliedAt": {
                      "type": "string",
                      "nullable": true,
                      "description": "First-occurrence (MIN) ISO 8601 timestamp of a replied event in this scope; null if it never happened in scope (always null for postmark — no reply tracking)"
                    },
                    "firstBouncedAt": {
                      "type": "string",
                      "nullable": true,
                      "description": "First-occurrence (MIN) ISO 8601 timestamp of a bounced event in this scope; null if it never happened in scope"
                    },
                    "firstUnsubscribedAt": {
                      "type": "string",
                      "nullable": true,
                      "description": "First-occurrence (MIN) ISO 8601 timestamp of an unsubscribed event in this scope; null if it never happened in scope"
                    },
                    "sentCount": {
                      "type": "integer",
                      "minimum": 0,
                      "description": "Number of emails actually sent to the recipient in this scope (send-event count, not a boolean); 0 when no send. Sequence position derives from it: 1 = initial, 2 = first follow-up, 3 = second follow-up. Optional in contract v1; brand scope = SUM across the brand's campaigns."
                    },
                    "replyKind": {
                      "type": "string",
                      "nullable": true,
                      "enum": [
                        "lead_interested",
                        "lead_referral",
                        "lead_info_requested",
                        "lead_meeting_requested",
                        "lead_not_interested",
                        "lead_wrong_person",
                        "lead_changed_job",
                        "lead_neutral",
                        "lead_out_of_office",
                        "auto_reply_received",
                        null
                      ],
                      "description": "WHICH reply this is — the finer reading of `replyClassification`, from the human statement when one exists and from the automatic classification otherwise. Null when no reply kind is on record."
                    },
                    "disqualified": {
                      "type": "boolean",
                      "description": "True iff this person is PERMANENTLY out: they are not the right contact (`lead_wrong_person`) or they have left the role (`lead_changed_job`). A prospect who simply declines today (`lead_not_interested`) is NOT disqualified — the lead stays recyclable. Strictly a function of `replyKind`, so the two fields never disagree; false whenever no kind is on record, because an absence is not a disqualification."
                    }
                  },
                  "required": [
                    "contacted",
                    "sent",
                    "delivered",
                    "opened",
                    "clicked",
                    "replied",
                    "replyClassification",
                    "bounced",
                    "unsubscribed",
                    "lastDeliveredAt",
                    "replyKind",
                    "disqualified"
                  ],
                  "description": "Campaign-scoped status — present only when campaignId is provided"
                },
                "global": {
                  "type": "object",
                  "properties": {
                    "email": {
                      "type": "object",
                      "properties": {
                        "bounced": {
                          "type": "boolean",
                          "description": "True if this email bounced anywhere in the org"
                        },
                        "unsubscribed": {
                          "type": "boolean",
                          "description": "True if this email unsubscribed anywhere in the org"
                        }
                      },
                      "required": [
                        "bounced",
                        "unsubscribed"
                      ],
                      "description": "Global email signals (technical/legal)"
                    }
                  },
                  "required": [
                    "email"
                  ]
                }
              },
              "required": [
                "email",
                "byCampaign",
                "brand",
                "campaign",
                "global"
              ]
            }
          }
        },
        "required": [
          "results"
        ],
        "example": {
          "results": [
            {
              "email": "alice@media.com",
              "byCampaign": {
                "c1a2b3c4-0000-0000-0000-000000000001": {
                  "contacted": true,
                  "sent": true,
                  "delivered": true,
                  "opened": true,
                  "clicked": false,
                  "replied": false,
                  "replyClassification": null,
                  "replyKind": null,
                  "disqualified": false,
                  "bounced": false,
                  "unsubscribed": false,
                  "cancelled": false,
                  "lastDeliveredAt": "2026-03-01T10:00:00.000Z"
                },
                "c1a2b3c4-0000-0000-0000-000000000002": {
                  "contacted": true,
                  "sent": true,
                  "delivered": true,
                  "opened": false,
                  "clicked": true,
                  "replied": true,
                  "replyClassification": "positive",
                  "replyKind": "lead_interested",
                  "disqualified": false,
                  "bounced": false,
                  "unsubscribed": false,
                  "cancelled": false,
                  "lastDeliveredAt": "2026-03-02T12:00:00.000Z"
                }
              },
              "brand": {
                "contacted": true,
                "sent": true,
                "delivered": true,
                "opened": true,
                "clicked": true,
                "replied": true,
                "replyClassification": "positive",
                "replyKind": "lead_interested",
                "disqualified": false,
                "bounced": false,
                "unsubscribed": false,
                "cancelled": false,
                "lastDeliveredAt": "2026-03-02T12:00:00.000Z"
              },
              "campaign": null,
              "global": {
                "email": {
                  "bounced": false,
                  "unsubscribed": false
                }
              }
            }
          ]
        }
      },
      "StatusRequest": {
        "type": "object",
        "properties": {
          "brandId": {
            "type": "string",
            "description": "Brand ID — when provided without campaignId, returns per-campaign breakdown + aggregated brand status"
          },
          "campaignId": {
            "type": "string",
            "description": "Campaign ID — when provided, returns campaign-scoped status (brandId is ignored)"
          },
          "items": {
            "type": "array",
            "items": {
              "type": "object",
              "properties": {
                "email": {
                  "type": "string",
                  "description": "Email address"
                }
              },
              "required": [
                "email"
              ]
            },
            "minItems": 1,
            "description": "Emails to check"
          }
        },
        "required": [
          "items"
        ],
        "example": {
          "brandId": "b8f0e2a1-1234-4abc-9def-000000000001",
          "items": [
            {
              "email": "alice@media.com"
            },
            {
              "email": "bob@test.com"
            }
          ]
        }
      },
      "LeadOptOutCreateResponse": {
        "type": "object",
        "properties": {
          "idempotent": {
            "type": "boolean",
            "description": "True if a standing opt-out already existed — no new record, no repeated side effects"
          },
          "campaignsAffected": {
            "type": "integer",
            "description": "Campaigns of this org holding that address"
          },
          "campaignsStopped": {
            "type": "integer",
            "description": "How many of them could be stopped at the SENDER. Below campaignsAffected means a pause failed and is logged — the local stop and the opt-out record still hold."
          },
          "optOut": {
            "type": "object",
            "properties": {
              "id": {
                "type": "string"
              },
              "orgId": {
                "type": "string"
              },
              "email": {
                "type": "string"
              },
              "channel": {
                "type": "string",
                "enum": [
                  "sms",
                  "phone_call",
                  "email_reply",
                  "forwarded_thread",
                  "in_person",
                  "web_form",
                  "other"
                ],
                "description": "The channel the person used to ask us to stop. Required — this is what makes the record auditable as a consent record rather than a flag."
              },
              "statedBy": {
                "type": "string",
                "description": "The staff member who recorded it (x-user-id)"
              },
              "notes": {
                "type": "string",
                "nullable": true
              },
              "statedAt": {
                "type": "string",
                "description": "ISO 8601 timestamp"
              },
              "withdrawnAt": {
                "type": "string",
                "nullable": true,
                "description": "ISO 8601 timestamp of the withdrawal, or null while the record still STANDS. Non-null means it was taken back: it is kept for audit and must never be rendered as a current opt-out."
              },
              "withdrawnBy": {
                "type": "string",
                "nullable": true
              }
            },
            "required": [
              "id",
              "orgId",
              "email",
              "channel",
              "statedBy",
              "notes",
              "statedAt",
              "withdrawnAt",
              "withdrawnBy"
            ]
          }
        },
        "required": [
          "idempotent",
          "campaignsAffected",
          "campaignsStopped",
          "optOut"
        ]
      },
      "LeadOptOutCreateBody": {
        "type": "object",
        "properties": {
          "email": {
            "type": "string",
            "format": "email",
            "description": "The person who asked us to stop"
          },
          "channel": {
            "type": "string",
            "enum": [
              "sms",
              "phone_call",
              "email_reply",
              "forwarded_thread",
              "in_person",
              "web_form",
              "other"
            ],
            "description": "The channel the person used to ask us to stop. Required — this is what makes the record auditable as a consent record rather than a flag."
          },
          "notes": {
            "type": "string",
            "maxLength": 2000,
            "description": "Optional free-text human note for audit"
          }
        },
        "required": [
          "email",
          "channel"
        ],
        "example": {
          "email": "alice@media.com",
          "channel": "sms",
          "notes": "Texted my mobile asking not to be contacted again"
        }
      },
      "LeadOptOutWithdrawResponse": {
        "type": "object",
        "properties": {
          "campaignsAffected": {
            "type": "integer"
          },
          "optOut": {
            "type": "object",
            "properties": {
              "id": {
                "type": "string"
              },
              "orgId": {
                "type": "string"
              },
              "email": {
                "type": "string"
              },
              "channel": {
                "type": "string",
                "enum": [
                  "sms",
                  "phone_call",
                  "email_reply",
                  "forwarded_thread",
                  "in_person",
                  "web_form",
                  "other"
                ],
                "description": "The channel the person used to ask us to stop. Required — this is what makes the record auditable as a consent record rather than a flag."
              },
              "statedBy": {
                "type": "string",
                "description": "The staff member who recorded it (x-user-id)"
              },
              "notes": {
                "type": "string",
                "nullable": true
              },
              "statedAt": {
                "type": "string",
                "description": "ISO 8601 timestamp"
              },
              "withdrawnAt": {
                "type": "string",
                "nullable": true,
                "description": "ISO 8601 timestamp of the withdrawal, or null while the record still STANDS. Non-null means it was taken back: it is kept for audit and must never be rendered as a current opt-out."
              },
              "withdrawnBy": {
                "type": "string",
                "nullable": true
              }
            },
            "required": [
              "id",
              "orgId",
              "email",
              "channel",
              "statedBy",
              "notes",
              "statedAt",
              "withdrawnAt",
              "withdrawnBy"
            ],
            "description": "The record that was withdrawn, now carrying withdrawnAt / withdrawnBy."
          }
        },
        "required": [
          "campaignsAffected",
          "optOut"
        ]
      },
      "LeadOptOutWithdrawError": {
        "type": "object",
        "properties": {
          "error": {
            "type": "string"
          },
          "code": {
            "type": "string",
            "enum": [
              "no_standing_optout"
            ],
            "description": "Nothing currently stands for this lead — nothing was ever recorded, or it is already withdrawn."
          }
        },
        "required": [
          "error",
          "code"
        ]
      },
      "LeadOptOutWithdrawBody": {
        "type": "object",
        "properties": {
          "email": {
            "type": "string",
            "format": "email",
            "description": "The person whose opt-out is being taken back"
          },
          "notes": {
            "type": "string",
            "maxLength": 2000,
            "description": "Optional free-text human note for audit"
          }
        },
        "required": [
          "email"
        ],
        "example": {
          "email": "alice@media.com",
          "notes": "Recorded on the wrong lead"
        }
      },
      "LeadOptOutListResponse": {
        "type": "object",
        "properties": {
          "optOuts": {
            "type": "array",
            "items": {
              "type": "object",
              "properties": {
                "id": {
                  "type": "string"
                },
                "orgId": {
                  "type": "string"
                },
                "email": {
                  "type": "string"
                },
                "channel": {
                  "type": "string",
                  "enum": [
                    "sms",
                    "phone_call",
                    "email_reply",
                    "forwarded_thread",
                    "in_person",
                    "web_form",
                    "other"
                  ],
                  "description": "The channel the person used to ask us to stop. Required — this is what makes the record auditable as a consent record rather than a flag."
                },
                "statedBy": {
                  "type": "string",
                  "description": "The staff member who recorded it (x-user-id)"
                },
                "notes": {
                  "type": "string",
                  "nullable": true
                },
                "statedAt": {
                  "type": "string",
                  "description": "ISO 8601 timestamp"
                },
                "withdrawnAt": {
                  "type": "string",
                  "nullable": true,
                  "description": "ISO 8601 timestamp of the withdrawal, or null while the record still STANDS. Non-null means it was taken back: it is kept for audit and must never be rendered as a current opt-out."
                },
                "withdrawnBy": {
                  "type": "string",
                  "nullable": true
                }
              },
              "required": [
                "id",
                "orgId",
                "email",
                "channel",
                "statedBy",
                "notes",
                "statedAt",
                "withdrawnAt",
                "withdrawnBy"
              ]
            }
          }
        },
        "required": [
          "optOuts"
        ]
      },
      "SendingForecastResponse": {
        "type": "object",
        "properties": {
          "asOf": {
            "type": "string",
            "description": "ISO8601 timestamp of computation"
          },
          "dailyCapacity": {
            "type": "integer",
            "description": "Emails/day the fleet can send — Σ daily send limit over accounts whose lifecycle_status == 'in_production' (the live send gate)"
          },
          "healthyAccountCount": {
            "type": "integer",
            "description": "Accounts currently in_production (send-eligible)"
          },
          "totalAccountCount": {
            "type": "integer",
            "description": "All accounts in the shared workspace before filtering"
          },
          "blockedDomainCount": {
            "type": "integer",
            "description": "Accounts blocked by domain policy (lifecycle deactivated_by_user)"
          },
          "days": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/ForecastDay"
            },
            "description": "Per-day scheduled send volume from today forward, chronological. Bounded: stops when the active-campaign backlog drains. May be [] when nothing is scheduled."
          }
        },
        "required": [
          "asOf",
          "dailyCapacity",
          "healthyAccountCount",
          "totalAccountCount",
          "blockedDomainCount",
          "days"
        ]
      },
      "ForecastDay": {
        "type": "object",
        "properties": {
          "date": {
            "type": "string",
            "description": "Calendar day, YYYY-MM-DD (UTC)"
          },
          "scheduledCount": {
            "type": "integer",
            "description": "Emails scheduled to send that day across the whole fleet"
          }
        },
        "required": [
          "date",
          "scheduledCount"
        ]
      },
      "AccountHealthResponse": {
        "type": "object",
        "properties": {
          "asOf": {
            "type": "string",
            "description": "ISO8601 timestamp of computation"
          },
          "accounts": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/AccountHealth"
            },
            "description": "Per-account deliverability health across the shared workspace. Always present; may be []."
          }
        },
        "required": [
          "asOf",
          "accounts"
        ]
      },
      "AccountHealth": {
        "type": "object",
        "properties": {
          "email": {
            "type": "string",
            "description": "Sending account email"
          },
          "domain": {
            "type": "string",
            "nullable": true,
            "description": "Sending domain (part after @); null if the email is malformed"
          },
          "status": {
            "type": "string",
            "description": "Existing status representation — 'active' when Instantly status > 0, else 'inactive'"
          },
          "warmupScore": {
            "type": "integer",
            "nullable": true,
            "description": "Instantly Health Score stat_warmup_score (0-100); null if unknown"
          },
          "dailyLimit": {
            "type": "integer",
            "nullable": true,
            "description": "Per-account daily MAX-SEND limit (cold-send cap); null if unknown"
          },
          "effectiveDailyCap": {
            "type": "integer",
            "nullable": true,
            "description": "The daily cap send SELECTION actually compares this account's load against: min(dailyLimit, age ramp) at asOf. Equal to dailyLimit for a mature mailbox, strictly lower for one under 4 weeks old (whose real per-user quota is far below the fleet cap). Rendering dailyLimit alone reads 50 for an account the selector holds at 23. Null when the account carries no daily limit at all."
          },
          "fillRank": {
            "type": "integer",
            "nullable": true,
            "description": "1-based position of this account in the send-selection fill order over the in-production pool — rank 1 is the mailbox a NEW sequence is offered first, and the fleet saturates it before touching rank 2. Null when the account is not in that pool (blocked / not in_production, or reserved to a feature slug). Never fabricated: an account the selector would not consider has no position in its order. Followups stay pinned to their originating account, so a high rank still DISPATCHES for a full sequence length after it stops being assigned."
          },
          "warmupLimit": {
            "type": "integer",
            "nullable": true,
            "description": "Per-account daily WARMUP send volume — Instantly warmup.limit, the warm-up emails/day target. DISTINCT from dailyLimit (the max-send cap): a live account often runs warmup 10/day while its send cap is 50/day. Null when Instantly reports no warmup config."
          },
          "blocked": {
            "type": "boolean",
            "description": "True when the account is NOT send-eligible (lifecycle_status != 'in_production')"
          },
          "blockReason": {
            "type": "string",
            "nullable": true,
            "description": "Short reason when blocked — the account's lifecycle_status (in_recovery / deactivated_by_instantly / deactivated_by_user), or 'unclassified' when the lifecycle has not yet run; null when in_production."
          },
          "lifecycleStatus": {
            "type": "string",
            "nullable": true,
            "enum": [
              "in_production",
              "in_recovery",
              "deactivated_by_instantly",
              "deactivated_by_user",
              null
            ],
            "description": "Auto-derived lifecycle state; null until reconcileLifecycle first runs"
          },
          "lifecycleReason": {
            "type": "string",
            "nullable": true,
            "description": "Snapshot reason on the latest lifecycle transition; null until classified"
          },
          "lifecycleUpdatedAt": {
            "type": "string",
            "nullable": true,
            "description": "ISO8601 timestamp of the latest lifecycle transition; null until classified"
          },
          "inboxPlacement": {
            "$ref": "#/components/schemas/InboxPlacement"
          },
          "sentToday": {
            "type": "integer",
            "description": "Real (non-inferred) email_sent events observed today (UTC) from this account, from our silver log — the N in an N/dailyLimit read. 0 when none today, never fabricated."
          },
          "sentYesterday": {
            "type": "integer",
            "description": "Real (non-inferred) email_sent events observed YESTERDAY — the full previous UTC calendar day — from this account, from our silver log. Same provenance as sentToday. 0 when none, never fabricated."
          },
          "queueSize": {
            "type": "integer",
            "description": "Total queued STEPS for this account — every remaining un-sent email across all its queued sequences (still-provisioned sequence-cost holds on active campaigns attributed to this account, 1 campaign = 1 account). PARTITIONED by the four queued* date buckets: queueSize === queuedFirstUnsent + queuedNextToday + queuedNextTomorrow + queuedNextLater. 0 when nothing queued; unattributable steps excluded, never fabricated."
          },
          "queuedSequences": {
            "type": "integer",
            "description": "Distinct queued SEQUENCES (1 Instantly campaign = 1 lead = 1 sequence) attributed to this account — a DIFFERENT granularity from queueSize (which counts pending STEPS) and from the four date buckets (which partition STEPS). Both kept, both intentional. 0 when nothing queued; unattributable sequences excluded, never fabricated."
          },
          "queuedFirstUnsent": {
            "type": "integer",
            "description": "Q0-first — queued STEPS belonging to sequences whose first email has not been sent yet (no lastSentAt anchor → not date-projected, counted as 'not started')."
          },
          "queuedFirstUnsentSequences": {
            "type": "integer",
            "description": "Q0-first as SEQUENCES — never-contacted leads on this account, i.e. how many FIRST emails are actually due. This is the quantity SEND SELECTION counts toward today's load; queuedFirstUnsent counts every remaining step of those same sequences and therefore over-states 'today' by the whole future sequence. An ops 'queued today' figure should be queuedFirstUnsentSequences + queuedNextToday, compared against dailyLimit — that is the number the selector decides on."
          },
          "queuedNextToday": {
            "type": "integer",
            "description": "Q0-next — queued STEPS projected today (UTC) or overdue. Each step's date = lastSentAt + the CHAINED configured delays across every hop up to it (nominal-cadence LOWER BOUND that COMPOUNDS across steps; real Instantly dispatch slips later under throttling), so this reads as 'step DUE today-or-overdue', not a guaranteed send today."
          },
          "queuedOverdue": {
            "type": "integer",
            "description": "BACKLOG subset of queuedNextToday — queued STEPS whose nominal due date is STRICTLY BEFORE today (UTC), i.e. owed on an earlier day and never dispatched. Always <= queuedNextToday, and NOT part of the four-bucket partition of queueSize (it re-counts steps already in queuedNextToday — do not add it to the sum). Separates 'due today' from 'behind'; a rising value means Instantly is dispatching slower than we assign."
          },
          "queuedNextTomorrow": {
            "type": "integer",
            "description": "Q1-next — queued STEPS projected tomorrow (UTC) via the chained-delay projection."
          },
          "queuedNextLater": {
            "type": "integer",
            "description": "Q-next — queued STEPS projected after tomorrow (UTC) via the chained-delay projection."
          },
          "accountType": {
            "type": "string",
            "nullable": true,
            "description": "Connection provider from Instantly's provider_code — 'google' / 'microsoft' / 'imap'; null when unreported. This is the sending type, NOT the provisioning class (DFY-prewarmed vs legacy), which Instantly does not expose."
          }
        },
        "required": [
          "email",
          "domain",
          "status",
          "warmupScore",
          "dailyLimit",
          "effectiveDailyCap",
          "fillRank",
          "warmupLimit",
          "blocked",
          "blockReason",
          "lifecycleStatus",
          "lifecycleReason",
          "lifecycleUpdatedAt",
          "inboxPlacement",
          "sentToday",
          "sentYesterday",
          "queueSize",
          "queuedSequences",
          "queuedFirstUnsent",
          "queuedFirstUnsentSequences",
          "queuedNextToday",
          "queuedOverdue",
          "queuedNextTomorrow",
          "queuedNextLater",
          "accountType"
        ]
      },
      "InboxPlacement": {
        "type": "object",
        "nullable": true,
        "properties": {
          "inboxPct": {
            "type": "number",
            "description": "Percentage of test emails landing in inbox, pooled across every ESP of the latest test (sum inbox / sum seeds) — the same number the lifecycle delivery gate reads"
          },
          "spamPct": {
            "type": "number",
            "description": "Percentage landing in spam, pooled across every ESP"
          },
          "missingPct": {
            "type": "number",
            "description": "Percentage not delivered / missing, pooled across every ESP"
          },
          "testedAt": {
            "type": "string",
            "description": "ISO8601 timestamp of the placement test"
          }
        },
        "required": [
          "inboxPct",
          "spamPct",
          "missingPct",
          "testedAt"
        ],
        "description": "Inbox-placement breakdown from the account's latest placement test, promoted through our own Bronze/Silver/Gold pipeline. Headline percentages are the WORST gated ESP leg (the leg the lifecycle delivery gate reads), with the full per-ESP breakdown in `perEsp`. Null when the account has never been in a test — the Instantly V2 API exposes no standing per-account placement property, so this is derived from real test results and never fabricated."
      },
      "AccountHealthHistoryResponse": {
        "type": "object",
        "properties": {
          "email": {
            "type": "string",
            "description": "The queried sending account email"
          },
          "history": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/PlacementHistoryEntry"
            },
            "description": "Blended placement per test, newest first. [] when never tested."
          }
        },
        "required": [
          "email",
          "history"
        ]
      },
      "PlacementHistoryEntry": {
        "type": "object",
        "properties": {
          "testId": {
            "type": "string",
            "description": "Instantly inbox-placement test ID"
          },
          "inboxPct": {
            "type": "number",
            "description": "Percentage of seed emails landing in inbox"
          },
          "spamPct": {
            "type": "number",
            "description": "Percentage landing in spam"
          },
          "missingPct": {
            "type": "number",
            "description": "Percentage not delivered / missing"
          },
          "testedAt": {
            "type": "string",
            "description": "ISO8601 timestamp of the test"
          }
        },
        "required": [
          "testId",
          "inboxPct",
          "spamPct",
          "missingPct",
          "testedAt"
        ]
      },
      "EmailsBackfillAccepted": {
        "type": "object",
        "properties": {
          "accepted": {
            "type": "boolean"
          },
          "runId": {
            "type": "string",
            "description": "Background run identifier (watch logs)"
          }
        },
        "required": [
          "accepted",
          "runId"
        ]
      },
      "EmailsBackfillRequest": {
        "type": "object",
        "properties": {
          "maxPages": {
            "type": "integer",
            "minimum": 0,
            "exclusiveMinimum": true,
            "description": "Bound the walk to N pages (100 emails each). Omit to sweep the whole Unibox."
          },
          "startingAfter": {
            "type": "string",
            "description": "Resume from the cursor a previous run reported (its summary log line, or a progress line). Omit to start from the newest email."
          }
        }
      },
      "PlacementSyncAccepted": {
        "type": "object",
        "properties": {
          "accepted": {
            "type": "boolean"
          },
          "runId": {
            "type": "string",
            "description": "Background run identifier (watch logs)"
          }
        },
        "required": [
          "accepted",
          "runId"
        ]
      },
      "PlacementRunResponse": {
        "type": "object",
        "properties": {
          "created": {
            "type": "integer",
            "description": "One-time placement tests created this call (1, or 0 when no healthy senders)"
          },
          "testCode": {
            "type": "string",
            "nullable": true,
            "description": "test_code of the created one-time test; null when nothing was created"
          },
          "recipientEsps": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Recipient ESPs the test seeds (Google/Outlook)"
          },
          "senderCount": {
            "type": "integer",
            "description": "Number of live-send-eligible accounts the test seeds from"
          }
        },
        "required": [
          "created",
          "testCode",
          "recipientEsps",
          "senderCount"
        ]
      },
      "PlacementEnsureResponse": {
        "type": "object",
        "properties": {
          "existing": {
            "type": "integer",
            "description": "Automated placement tests already present"
          },
          "created": {
            "type": "integer",
            "description": "Automated placement tests created this call"
          },
          "perDay": {
            "type": "integer",
            "description": "Target automated tests per day"
          }
        },
        "required": [
          "existing",
          "created",
          "perDay"
        ]
      },
      "SeedPlacementRunRequest": {
        "type": "object",
        "properties": {
          "limit": {
            "type": "integer",
            "minimum": 0,
            "exclusiveMinimum": true,
            "description": "Cap the number of seed emails dispatched this run (batching / smoke test)"
          },
          "force": {
            "type": "boolean",
            "description": "Bypass the due check and run now. The cron never sets this."
          }
        }
      },
      "InfraSyncAccepted": {
        "type": "object",
        "properties": {
          "accepted": {
            "type": "boolean",
            "enum": [
              true
            ]
          },
          "runId": {
            "type": "string",
            "description": "Background run identifier (watch logs)"
          }
        },
        "required": [
          "accepted",
          "runId"
        ]
      },
      "InfraDomainsResponse": {
        "type": "object",
        "properties": {
          "asOf": {
            "type": "string"
          },
          "domains": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/InfraDomainRow"
            }
          }
        },
        "required": [
          "asOf",
          "domains"
        ]
      },
      "InfraDomainRow": {
        "type": "object",
        "properties": {
          "domain": {
            "type": "string"
          },
          "provider": {
            "type": "string",
            "description": "gandi | mailforge | primeforge | instantly-dfy"
          },
          "role": {
            "type": "string",
            "description": "registrar | mailbox | prewarm — what the vendor does for this domain"
          },
          "status": {
            "type": "string",
            "nullable": true,
            "description": "The vendor's own status string, verbatim"
          },
          "expiresAt": {
            "type": "string",
            "nullable": true
          },
          "autorenew": {
            "type": "boolean",
            "nullable": true,
            "description": "Null when the vendor exposes no such flag — distinct from false"
          },
          "deletionScheduled": {
            "type": "boolean"
          },
          "cancelledAt": {
            "type": "string",
            "nullable": true
          },
          "absentSince": {
            "type": "string",
            "nullable": true,
            "description": "Set when the vendor stopped reporting the domain; the row is kept, never deleted"
          },
          "vendorMailboxes": {
            "type": "integer",
            "description": "Mailboxes the VENDOR hosts — routinely differs from the Instantly account count on relayed domains"
          },
          "instantlyAccounts": {
            "type": "integer",
            "description": "Live Instantly sending accounts on this domain (ghosts excluded)"
          },
          "inProductionAccounts": {
            "type": "integer"
          },
          "sentLast30d": {
            "type": "integer",
            "description": "Real (non-inferred) dispatches over the trailing 30 days"
          },
          "monthlyCostCents": {
            "type": "number",
            "nullable": true,
            "description": "Null when nothing prices this domain — never a substitute figure"
          },
          "currency": {
            "type": "string",
            "nullable": true
          },
          "costSource": {
            "type": "string",
            "nullable": true,
            "description": "api (vendor-reported) | rate-card (versioned local row)"
          },
          "costPerEmailCents": {
            "type": "number",
            "nullable": true
          },
          "recurringMonthlyCents": {
            "type": "number",
            "nullable": true,
            "description": "The mailbox subscription — stops billing the moment the domain is cancelled"
          },
          "renewalCents": {
            "type": "number",
            "nullable": true,
            "description": "The YEARLY registration, already paid until renewalAt; deleting today avoids it then, it refunds nothing now"
          },
          "renewalAt": {
            "type": "string",
            "nullable": true,
            "description": "When that renewal falls due"
          }
        },
        "required": [
          "domain",
          "provider",
          "role",
          "status",
          "expiresAt",
          "autorenew",
          "deletionScheduled",
          "cancelledAt",
          "absentSince",
          "vendorMailboxes",
          "instantlyAccounts",
          "inProductionAccounts",
          "sentLast30d",
          "monthlyCostCents",
          "currency",
          "costSource",
          "costPerEmailCents",
          "recurringMonthlyCents",
          "renewalCents",
          "renewalAt"
        ]
      },
      "InfraWasteResponse": {
        "type": "object",
        "properties": {
          "asOf": {
            "type": "string"
          },
          "findingCount": {
            "type": "integer"
          },
          "findings": {
            "type": "array",
            "items": {
              "type": "object",
              "properties": {
                "domain": {
                  "type": "string"
                },
                "provider": {
                  "type": "string"
                },
                "reason": {
                  "type": "string",
                  "description": "paid_no_sending_accounts | cancelled_by_vendor | deletion_scheduled | expiring_within_30d"
                },
                "detail": {
                  "type": "string"
                },
                "monthlyCostCents": {
                  "type": "number",
                  "nullable": true
                },
                "currency": {
                  "type": "string",
                  "nullable": true
                },
                "expiresAt": {
                  "type": "string",
                  "nullable": true
                }
              },
              "required": [
                "domain",
                "provider",
                "reason",
                "detail",
                "monthlyCostCents",
                "currency",
                "expiresAt"
              ]
            }
          }
        },
        "required": [
          "asOf",
          "findingCount",
          "findings"
        ]
      },
      "InfraSpendResponse": {
        "type": "object",
        "properties": {
          "asOf": {
            "type": "string"
          },
          "byProvider": {
            "type": "array",
            "items": {
              "type": "object",
              "properties": {
                "provider": {
                  "type": "string"
                },
                "domainCount": {
                  "type": "integer"
                },
                "mailboxCount": {
                  "type": "integer"
                },
                "monthlyCents": {
                  "type": "number"
                },
                "currency": {
                  "type": "string"
                },
                "source": {
                  "type": "string",
                  "description": "api | rate-card | mixed"
                }
              },
              "required": [
                "provider",
                "domainCount",
                "mailboxCount",
                "monthlyCents",
                "currency",
                "source"
              ]
            }
          },
          "monthlyByCurrency": {
            "type": "array",
            "items": {
              "type": "object",
              "properties": {
                "currency": {
                  "type": "string"
                },
                "cents": {
                  "type": "number"
                }
              },
              "required": [
                "currency",
                "cents"
              ]
            }
          },
          "unpricedProviders": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Vendors whose domains we hold and cannot price at all — their cost is MISSING from the totals, not estimated into them"
          },
          "unpricedDomainCount": {
            "type": "integer"
          },
          "planSubscriptions": {
            "type": "array",
            "items": {
              "type": "object",
              "properties": {
                "item": {
                  "type": "string"
                },
                "monthlyCents": {
                  "type": "number"
                },
                "currency": {
                  "type": "string"
                },
                "note": {
                  "type": "string",
                  "nullable": true
                }
              },
              "required": [
                "item",
                "monthlyCents",
                "currency",
                "note"
              ]
            }
          },
          "sentLast30d": {
            "type": "integer"
          },
          "costPerEmailByCurrency": {
            "type": "array",
            "items": {
              "type": "object",
              "properties": {
                "currency": {
                  "type": "string"
                },
                "cents": {
                  "type": "number"
                }
              },
              "required": [
                "currency",
                "cents"
              ]
            }
          }
        },
        "required": [
          "asOf",
          "byProvider",
          "monthlyByCurrency",
          "unpricedProviders",
          "unpricedDomainCount",
          "planSubscriptions",
          "sentLast30d",
          "costPerEmailByCurrency"
        ]
      }
    },
    "parameters": {}
  },
  "paths": {
    "/": {
      "get": {
        "summary": "Service info",
        "responses": {
          "200": {
            "description": "Service info",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/RootResponse"
                }
              }
            }
          }
        }
      }
    },
    "/health": {
      "get": {
        "summary": "Health check",
        "responses": {
          "200": {
            "description": "Service is healthy",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HealthResponse"
                }
              }
            }
          }
        }
      }
    },
    "/webhooks/instantly/config": {
      "get": {
        "summary": "Get webhook URL for BYOK configuration",
        "description": "Returns the webhook URL that BYOK customers should paste into their Instantly dashboard webhook settings.",
        "responses": {
          "200": {
            "description": "Webhook configuration",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/WebhookConfigResponse"
                }
              }
            }
          },
          "500": {
            "description": "INSTANTLY_SERVICE_URL not configured",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Error"
                }
              }
            }
          }
        }
      }
    },
    "/webhooks/instantly": {
      "post": {
        "summary": "Receive Instantly webhook events",
        "description": "Verification: the campaign_id in the payload must exist in the database. Each campaign UUID is unguessable and stored with its org on creation.",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/WebhookPayload"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "Webhook processed",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/WebhookResponse"
                }
              }
            }
          }
        }
      }
    },
    "/u/{payload}/{signature}": {
      "get": {
        "summary": "Render the opt-out confirmation for a self-sent email",
        "description": "Public and unauthenticated — a prospect clicks it from their inbox — so the HMAC in the URL is the entire gate. This endpoint does NOT unsubscribe: corporate link scanners fetch every URL in an inbound email before the human sees it, so acting on GET would opt out prospects who never clicked. It returns an HTML page carrying a POST form. An invalid signature and an unknown campaign both return 404, so the route cannot enumerate campaigns.",
        "parameters": [
          {
            "schema": {
              "type": "string",
              "description": "base64url of `<instantlyCampaignId>:<leadEmail>`"
            },
            "required": true,
            "description": "base64url of `<instantlyCampaignId>:<leadEmail>`",
            "name": "payload",
            "in": "path"
          },
          {
            "schema": {
              "type": "string",
              "description": "base64url HMAC-SHA256 of the payload"
            },
            "required": true,
            "description": "base64url HMAC-SHA256 of the payload",
            "name": "signature",
            "in": "path"
          }
        ],
        "responses": {
          "200": {
            "description": "HTML confirmation page",
            "content": {
              "text/html": {
                "schema": {
                  "type": "string"
                }
              }
            }
          },
          "404": {
            "description": "Invalid signature or malformed payload",
            "content": {
              "text/html": {
                "schema": {
                  "type": "string"
                }
              }
            }
          }
        }
      },
      "post": {
        "summary": "Opt a recipient out of a self-sent sequence",
        "description": "Performs the opt-out, and is also the RFC 8058 one-click target advertised by the List-Unsubscribe-Post header. Promotes a real `lead_unsubscribed` silver event through the shared promoteEvent path, so the existing machinery stops the sequence, cancels the remaining provisioned holds and marks the campaign terminal.",
        "parameters": [
          {
            "schema": {
              "type": "string",
              "description": "base64url of `<instantlyCampaignId>:<leadEmail>`"
            },
            "required": true,
            "description": "base64url of `<instantlyCampaignId>:<leadEmail>`",
            "name": "payload",
            "in": "path"
          },
          {
            "schema": {
              "type": "string",
              "description": "base64url HMAC-SHA256 of the payload"
            },
            "required": true,
            "description": "base64url HMAC-SHA256 of the payload",
            "name": "signature",
            "in": "path"
          }
        ],
        "responses": {
          "200": {
            "description": "HTML confirmation that the recipient was removed",
            "content": {
              "text/html": {
                "schema": {
                  "type": "string"
                }
              }
            }
          },
          "404": {
            "description": "Invalid signature or malformed payload",
            "content": {
              "text/html": {
                "schema": {
                  "type": "string"
                }
              }
            }
          }
        }
      }
    },
    "/c/{payload}/{signature}": {
      "get": {
        "summary": "Record a click and redirect to its real destination",
        "description": "Public and unauthenticated — a prospect follows it from their inbox — so the HMAC in the URL is the entire gate. The destination is INSIDE the signed payload, never a query parameter: a redirector that forwards to whatever a caller supplies is an open redirect, which would let anyone borrow the domain to bounce victims at a phishing page and get it blacklisted. Records the hit in bronze, promotes `email_link_clicked` (which is what stop-on-click fires on), then 302s. An invalid signature, a malformed payload and a non-http destination all return 404 identically.",
        "parameters": [
          {
            "schema": {
              "type": "string"
            },
            "required": true,
            "name": "payload",
            "in": "path"
          },
          {
            "schema": {
              "type": "string"
            },
            "required": true,
            "name": "signature",
            "in": "path"
          }
        ],
        "responses": {
          "302": {
            "description": "Redirect to the signed destination"
          },
          "404": {
            "description": "Invalid signature, malformed payload, or unusable destination",
            "content": {
              "text/html": {
                "schema": {
                  "type": "string"
                }
              }
            }
          }
        }
      }
    },
    "/internal/self-send/dispatch": {
      "post": {
        "summary": "Send the steps now due on the self-hosted SMTP transport",
        "description": "Platform-scoped ops sweep. Reads the still-provisioned cost ledger for campaigns frozen to send_transport='smtp', picks each lead's next due step from the shared cadence, clips to what each mailbox can still send today (min(daily_limit, rampCapForAge) minus real sends so far), dispatches, and promotes a real `email_sent`. Idempotent: a step leaves the due set as soon as its hold is actualized. Returns 202 and runs in the background — watch for `self-send-dispatch: done`. 409 when SELF_SEND_DISPATCH_ENABLED is not 'true'.",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/SelfSendDispatchRequest"
              }
            }
          }
        },
        "responses": {
          "202": {
            "description": "Sweep accepted; runs in the background",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/SelfSendDispatchAccepted"
                }
              }
            }
          },
          "409": {
            "description": "Disabled (SELF_SEND_DISPATCH_ENABLED is not 'true')",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Error"
                }
              }
            }
          }
        }
      }
    },
    "/internal/self-send/poll": {
      "post": {
        "summary": "Read the self-send mailboxes and ingest replies and bounces",
        "description": "Platform-scoped ops sweep. Connects to each mailbox on send_transport='smtp' over IMAP, correlates what it finds against the Message-Ids we know we sent, and promotes `reply_received`, `auto_reply_received` or `email_bounced` into silver. An autoresponder is deliberately NOT a reply: `reply_received` stops the sequence and cancels the lead's remaining holds, so filing an out-of-office as one would end the outreach for a prospect who never engaged. Idempotent without a cursor — each run re-reads an overlapping window and the unique (account, message_id) bronze index absorbs the overlap. Returns 202 and runs in the background; watch for `self-send-poll: done`. 409 when SELF_SEND_DISPATCH_ENABLED is not 'true'.",
        "responses": {
          "202": {
            "description": "Poll accepted; runs in the background",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/SelfSendDispatchAccepted"
                }
              }
            }
          },
          "409": {
            "description": "Disabled (SELF_SEND_DISPATCH_ENABLED is not 'true')",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Error"
                }
              }
            }
          }
        }
      }
    },
    "/orgs/send": {
      "post": {
        "summary": "Send email via Instantly campaign",
        "description": "Brand IDs, campaign ID, and workflow slug are read from headers (x-brand-id, x-campaign-id, x-workflow-slug) — do NOT pass them in the body.",
        "parameters": [
          {
            "schema": {
              "type": "string",
              "description": "Campaign ID — automatically injected by workflow-service on all DAG calls"
            },
            "required": false,
            "description": "Campaign ID — automatically injected by workflow-service on all DAG calls",
            "name": "x-campaign-id",
            "in": "header"
          },
          {
            "schema": {
              "type": "string",
              "description": "Brand ID(s) — comma-separated UUIDs, automatically injected by workflow-service on all DAG calls. Example: uuid1,uuid2,uuid3"
            },
            "required": false,
            "description": "Brand ID(s) — comma-separated UUIDs, automatically injected by workflow-service on all DAG calls. Example: uuid1,uuid2,uuid3",
            "name": "x-brand-id",
            "in": "header"
          },
          {
            "schema": {
              "type": "string",
              "description": "Workflow slug — automatically injected by workflow-service on all DAG calls"
            },
            "required": false,
            "description": "Workflow slug — automatically injected by workflow-service on all DAG calls",
            "name": "x-workflow-slug",
            "in": "header"
          },
          {
            "schema": {
              "type": "string",
              "description": "Feature slug — propagated through the full call chain for tracking"
            },
            "required": false,
            "description": "Feature slug — propagated through the full call chain for tracking",
            "name": "x-feature-slug",
            "in": "header"
          },
          {
            "schema": {
              "type": "string",
              "description": "Explicit active goal attribution. Stored only when supplied; never inferred."
            },
            "required": false,
            "description": "Explicit active goal attribution. Stored only when supplied; never inferred.",
            "name": "x-goal",
            "in": "header"
          },
          {
            "schema": {
              "type": "string",
              "description": "Explicit brand-profile attribution. Stored only when supplied; never inferred."
            },
            "required": false,
            "description": "Explicit brand-profile attribution. Stored only when supplied; never inferred.",
            "name": "x-brand-profile-id",
            "in": "header"
          },
          {
            "schema": {
              "type": "string",
              "description": "Explicit audience attribution (human-service audience.id). Stored only when supplied; never inferred."
            },
            "required": false,
            "description": "Explicit audience attribution (human-service audience.id). Stored only when supplied; never inferred.",
            "name": "x-audience-id",
            "in": "header"
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/SendRequest"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "Email sent",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/SendResponse"
                }
              }
            }
          },
          "400": {
            "description": "Missing required fields",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Error"
                }
              }
            }
          },
          "401": {
            "description": "Unauthorized"
          },
          "409": {
            "description": "Refused, not a transport failure — no email was sent and nothing was billed. `code: \"recent_brand_contact\"` = the recipient was already emailed for one of this send's brands inside the 3-month re-contact window. `code: \"lead_id_conflict\"` = the email already exists under a different lead_id.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/SendRefusal"
                }
              }
            }
          },
          "500": {
            "description": "Failed to send",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Error"
                }
              }
            }
          }
        }
      }
    },
    "/orgs/campaigns/{campaignId}": {
      "get": {
        "summary": "Get a campaign",
        "parameters": [
          {
            "schema": {
              "type": "string"
            },
            "required": true,
            "name": "campaignId",
            "in": "path"
          },
          {
            "schema": {
              "type": "string",
              "description": "Campaign ID — automatically injected by workflow-service on all DAG calls"
            },
            "required": false,
            "description": "Campaign ID — automatically injected by workflow-service on all DAG calls",
            "name": "x-campaign-id",
            "in": "header"
          },
          {
            "schema": {
              "type": "string",
              "description": "Brand ID(s) — comma-separated UUIDs, automatically injected by workflow-service on all DAG calls. Example: uuid1,uuid2,uuid3"
            },
            "required": false,
            "description": "Brand ID(s) — comma-separated UUIDs, automatically injected by workflow-service on all DAG calls. Example: uuid1,uuid2,uuid3",
            "name": "x-brand-id",
            "in": "header"
          },
          {
            "schema": {
              "type": "string",
              "description": "Workflow slug — automatically injected by workflow-service on all DAG calls"
            },
            "required": false,
            "description": "Workflow slug — automatically injected by workflow-service on all DAG calls",
            "name": "x-workflow-slug",
            "in": "header"
          },
          {
            "schema": {
              "type": "string",
              "description": "Feature slug — propagated through the full call chain for tracking"
            },
            "required": false,
            "description": "Feature slug — propagated through the full call chain for tracking",
            "name": "x-feature-slug",
            "in": "header"
          },
          {
            "schema": {
              "type": "string",
              "description": "Explicit active goal attribution. Stored only when supplied; never inferred."
            },
            "required": false,
            "description": "Explicit active goal attribution. Stored only when supplied; never inferred.",
            "name": "x-goal",
            "in": "header"
          },
          {
            "schema": {
              "type": "string",
              "description": "Explicit brand-profile attribution. Stored only when supplied; never inferred."
            },
            "required": false,
            "description": "Explicit brand-profile attribution. Stored only when supplied; never inferred.",
            "name": "x-brand-profile-id",
            "in": "header"
          },
          {
            "schema": {
              "type": "string",
              "description": "Explicit audience attribution (human-service audience.id). Stored only when supplied; never inferred."
            },
            "required": false,
            "description": "Explicit audience attribution (human-service audience.id). Stored only when supplied; never inferred.",
            "name": "x-audience-id",
            "in": "header"
          }
        ],
        "responses": {
          "200": {
            "description": "Campaign found"
          },
          "401": {
            "description": "Unauthorized"
          },
          "404": {
            "description": "Campaign not found",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Error"
                }
              }
            }
          }
        }
      }
    },
    "/orgs/campaigns": {
      "get": {
        "summary": "List campaigns for the authenticated org",
        "parameters": [
          {
            "schema": {
              "type": "string",
              "description": "Campaign ID — automatically injected by workflow-service on all DAG calls"
            },
            "required": false,
            "description": "Campaign ID — automatically injected by workflow-service on all DAG calls",
            "name": "x-campaign-id",
            "in": "header"
          },
          {
            "schema": {
              "type": "string",
              "description": "Brand ID(s) — comma-separated UUIDs, automatically injected by workflow-service on all DAG calls. Example: uuid1,uuid2,uuid3"
            },
            "required": false,
            "description": "Brand ID(s) — comma-separated UUIDs, automatically injected by workflow-service on all DAG calls. Example: uuid1,uuid2,uuid3",
            "name": "x-brand-id",
            "in": "header"
          },
          {
            "schema": {
              "type": "string",
              "description": "Workflow slug — automatically injected by workflow-service on all DAG calls"
            },
            "required": false,
            "description": "Workflow slug — automatically injected by workflow-service on all DAG calls",
            "name": "x-workflow-slug",
            "in": "header"
          },
          {
            "schema": {
              "type": "string",
              "description": "Feature slug — propagated through the full call chain for tracking"
            },
            "required": false,
            "description": "Feature slug — propagated through the full call chain for tracking",
            "name": "x-feature-slug",
            "in": "header"
          },
          {
            "schema": {
              "type": "string",
              "description": "Explicit active goal attribution. Stored only when supplied; never inferred."
            },
            "required": false,
            "description": "Explicit active goal attribution. Stored only when supplied; never inferred.",
            "name": "x-goal",
            "in": "header"
          },
          {
            "schema": {
              "type": "string",
              "description": "Explicit brand-profile attribution. Stored only when supplied; never inferred."
            },
            "required": false,
            "description": "Explicit brand-profile attribution. Stored only when supplied; never inferred.",
            "name": "x-brand-profile-id",
            "in": "header"
          },
          {
            "schema": {
              "type": "string",
              "description": "Explicit audience attribution (human-service audience.id). Stored only when supplied; never inferred."
            },
            "required": false,
            "description": "Explicit audience attribution (human-service audience.id). Stored only when supplied; never inferred.",
            "name": "x-audience-id",
            "in": "header"
          }
        ],
        "responses": {
          "200": {
            "description": "Campaigns list"
          },
          "401": {
            "description": "Unauthorized"
          }
        }
      }
    },
    "/orgs/campaigns/{campaignId}/status": {
      "patch": {
        "summary": "Update campaign status",
        "parameters": [
          {
            "schema": {
              "type": "string"
            },
            "required": true,
            "name": "campaignId",
            "in": "path"
          },
          {
            "schema": {
              "type": "string",
              "description": "Campaign ID — automatically injected by workflow-service on all DAG calls"
            },
            "required": false,
            "description": "Campaign ID — automatically injected by workflow-service on all DAG calls",
            "name": "x-campaign-id",
            "in": "header"
          },
          {
            "schema": {
              "type": "string",
              "description": "Brand ID(s) — comma-separated UUIDs, automatically injected by workflow-service on all DAG calls. Example: uuid1,uuid2,uuid3"
            },
            "required": false,
            "description": "Brand ID(s) — comma-separated UUIDs, automatically injected by workflow-service on all DAG calls. Example: uuid1,uuid2,uuid3",
            "name": "x-brand-id",
            "in": "header"
          },
          {
            "schema": {
              "type": "string",
              "description": "Workflow slug — automatically injected by workflow-service on all DAG calls"
            },
            "required": false,
            "description": "Workflow slug — automatically injected by workflow-service on all DAG calls",
            "name": "x-workflow-slug",
            "in": "header"
          },
          {
            "schema": {
              "type": "string",
              "description": "Feature slug — propagated through the full call chain for tracking"
            },
            "required": false,
            "description": "Feature slug — propagated through the full call chain for tracking",
            "name": "x-feature-slug",
            "in": "header"
          },
          {
            "schema": {
              "type": "string",
              "description": "Explicit active goal attribution. Stored only when supplied; never inferred."
            },
            "required": false,
            "description": "Explicit active goal attribution. Stored only when supplied; never inferred.",
            "name": "x-goal",
            "in": "header"
          },
          {
            "schema": {
              "type": "string",
              "description": "Explicit brand-profile attribution. Stored only when supplied; never inferred."
            },
            "required": false,
            "description": "Explicit brand-profile attribution. Stored only when supplied; never inferred.",
            "name": "x-brand-profile-id",
            "in": "header"
          },
          {
            "schema": {
              "type": "string",
              "description": "Explicit audience attribution (human-service audience.id). Stored only when supplied; never inferred."
            },
            "required": false,
            "description": "Explicit audience attribution (human-service audience.id). Stored only when supplied; never inferred.",
            "name": "x-audience-id",
            "in": "header"
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/UpdateStatusRequest"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "Status updated"
          },
          "400": {
            "description": "Invalid status",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Error"
                }
              }
            }
          },
          "401": {
            "description": "Unauthorized"
          },
          "404": {
            "description": "Campaign not found",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Error"
                }
              }
            }
          }
        }
      }
    },
    "/internal/campaigns/reconcile": {
      "post": {
        "summary": "Dispatch reconcile webhook state against Instantly API",
        "description": "Daily catch-up job that pulls Instantly's per-campaign state (aggregate, per-lead status, per-email records) and promotes any events missed by the webhook into the silver event log. Idempotent — safe to re-run.\n\nReturns 202 immediately and runs the job in the background. Verify completion via Railway logs (`reconcile: done`) or by polling `instantly_*_raw` bronze tables.",
        "responses": {
          "202": {
            "description": "Reconcile job dispatched (running in background)",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ReconcileAccepted"
                }
              }
            }
          },
          "401": {
            "description": "Unauthorized"
          }
        }
      }
    },
    "/internal/campaigns/retry-stuck": {
      "post": {
        "summary": "Dispatch daily retry-stuck sweep",
        "description": "Continuous worker that scans campaigns with `delivery_status='contacted'` stuck for >72h with no silver event proving Instantly ever sent. For each row: re-sends the lead onto a fresh healthy Instantly account, refunds the old cost rows, and provisions fresh costs against the new campaign. The row's local `delivery_status` stays `contacted` until a real `email_sent` webhook lands or it is terminally cancelled. The worker now runs continuously (not a daily cron) — this endpoint exists for legacy callers and is a no-op trigger.",
        "responses": {
          "202": {
            "description": "Retry-stuck sweep dispatched (running in background)",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ReconcileAccepted"
                }
              }
            }
          },
          "401": {
            "description": "Unauthorized"
          }
        }
      }
    },
    "/orgs/campaigns/{campaignId}/leads": {
      "get": {
        "summary": "List campaign leads",
        "parameters": [
          {
            "schema": {
              "type": "string"
            },
            "required": true,
            "name": "campaignId",
            "in": "path"
          },
          {
            "schema": {
              "type": "string"
            },
            "required": false,
            "name": "limit",
            "in": "query"
          },
          {
            "schema": {
              "type": "string"
            },
            "required": false,
            "name": "skip",
            "in": "query"
          },
          {
            "schema": {
              "type": "string",
              "description": "Campaign ID — automatically injected by workflow-service on all DAG calls"
            },
            "required": false,
            "description": "Campaign ID — automatically injected by workflow-service on all DAG calls",
            "name": "x-campaign-id",
            "in": "header"
          },
          {
            "schema": {
              "type": "string",
              "description": "Brand ID(s) — comma-separated UUIDs, automatically injected by workflow-service on all DAG calls. Example: uuid1,uuid2,uuid3"
            },
            "required": false,
            "description": "Brand ID(s) — comma-separated UUIDs, automatically injected by workflow-service on all DAG calls. Example: uuid1,uuid2,uuid3",
            "name": "x-brand-id",
            "in": "header"
          },
          {
            "schema": {
              "type": "string",
              "description": "Workflow slug — automatically injected by workflow-service on all DAG calls"
            },
            "required": false,
            "description": "Workflow slug — automatically injected by workflow-service on all DAG calls",
            "name": "x-workflow-slug",
            "in": "header"
          },
          {
            "schema": {
              "type": "string",
              "description": "Feature slug — propagated through the full call chain for tracking"
            },
            "required": false,
            "description": "Feature slug — propagated through the full call chain for tracking",
            "name": "x-feature-slug",
            "in": "header"
          },
          {
            "schema": {
              "type": "string",
              "description": "Explicit active goal attribution. Stored only when supplied; never inferred."
            },
            "required": false,
            "description": "Explicit active goal attribution. Stored only when supplied; never inferred.",
            "name": "x-goal",
            "in": "header"
          },
          {
            "schema": {
              "type": "string",
              "description": "Explicit brand-profile attribution. Stored only when supplied; never inferred."
            },
            "required": false,
            "description": "Explicit brand-profile attribution. Stored only when supplied; never inferred.",
            "name": "x-brand-profile-id",
            "in": "header"
          },
          {
            "schema": {
              "type": "string",
              "description": "Explicit audience attribution (human-service audience.id). Stored only when supplied; never inferred."
            },
            "required": false,
            "description": "Explicit audience attribution (human-service audience.id). Stored only when supplied; never inferred.",
            "name": "x-audience-id",
            "in": "header"
          }
        ],
        "responses": {
          "200": {
            "description": "Leads list"
          },
          "401": {
            "description": "Unauthorized"
          },
          "404": {
            "description": "Campaign not found",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Error"
                }
              }
            }
          }
        }
      }
    },
    "/orgs/stats": {
      "get": {
        "summary": "Get aggregated stats by filters",
        "description": "Aggregates stats from webhook events across campaigns matching the provided filters. Filters passed as query params; runIds is comma-separated.",
        "parameters": [
          {
            "schema": {
              "type": "string",
              "description": "Comma-separated list of run IDs"
            },
            "required": false,
            "description": "Comma-separated list of run IDs",
            "name": "runIds",
            "in": "query"
          },
          {
            "schema": {
              "type": "string",
              "description": "Filter by brand ID (matches campaigns containing this brand)"
            },
            "required": false,
            "description": "Filter by brand ID (matches campaigns containing this brand)",
            "name": "brandId",
            "in": "query"
          },
          {
            "schema": {
              "type": "string"
            },
            "required": false,
            "name": "campaignId",
            "in": "query"
          },
          {
            "schema": {
              "type": "string",
              "description": "Filter by explicit goal attribution stored in campaign metadata"
            },
            "required": false,
            "description": "Filter by explicit goal attribution stored in campaign metadata",
            "name": "goal",
            "in": "query"
          },
          {
            "schema": {
              "type": "string",
              "description": "Filter by explicit brand-profile attribution stored in campaign metadata"
            },
            "required": false,
            "description": "Filter by explicit brand-profile attribution stored in campaign metadata",
            "name": "brandProfileId",
            "in": "query"
          },
          {
            "schema": {
              "type": "string",
              "description": "Filter by explicit audience attribution stored in campaign metadata"
            },
            "required": false,
            "description": "Filter by explicit audience attribution stored in campaign metadata",
            "name": "audienceId",
            "in": "query"
          },
          {
            "schema": {
              "type": "string",
              "description": "Comma-separated list of workflow slugs to filter by"
            },
            "required": false,
            "description": "Comma-separated list of workflow slugs to filter by",
            "name": "workflowSlugs",
            "in": "query"
          },
          {
            "schema": {
              "type": "string",
              "description": "Comma-separated list of feature slugs to filter by"
            },
            "required": false,
            "description": "Comma-separated list of feature slugs to filter by",
            "name": "featureSlugs",
            "in": "query"
          },
          {
            "schema": {
              "type": "string",
              "enum": [
                "brandId",
                "campaignId",
                "workflowSlug",
                "featureSlug",
                "leadEmail",
                "audienceId",
                "day"
              ],
              "description": "Group results by dimension. groupBy=day keys buckets as YYYY-MM-DD in the requested timezone. Audience grouping uses only explicit campaign metadata."
            },
            "required": false,
            "description": "Group results by dimension. groupBy=day keys buckets as YYYY-MM-DD in the requested timezone. Audience grouping uses only explicit campaign metadata.",
            "name": "groupBy",
            "in": "query"
          },
          {
            "schema": {
              "type": "string",
              "description": "IANA timezone for groupBy=day buckets. Defaults to UTC."
            },
            "required": false,
            "description": "IANA timezone for groupBy=day buckets. Defaults to UTC.",
            "name": "timezone",
            "in": "query"
          },
          {
            "schema": {
              "type": "string",
              "description": "Campaign ID — automatically injected by workflow-service on all DAG calls"
            },
            "required": false,
            "description": "Campaign ID — automatically injected by workflow-service on all DAG calls",
            "name": "x-campaign-id",
            "in": "header"
          },
          {
            "schema": {
              "type": "string",
              "description": "Brand ID(s) — comma-separated UUIDs, automatically injected by workflow-service on all DAG calls. Example: uuid1,uuid2,uuid3"
            },
            "required": false,
            "description": "Brand ID(s) — comma-separated UUIDs, automatically injected by workflow-service on all DAG calls. Example: uuid1,uuid2,uuid3",
            "name": "x-brand-id",
            "in": "header"
          },
          {
            "schema": {
              "type": "string",
              "description": "Workflow slug — automatically injected by workflow-service on all DAG calls"
            },
            "required": false,
            "description": "Workflow slug — automatically injected by workflow-service on all DAG calls",
            "name": "x-workflow-slug",
            "in": "header"
          },
          {
            "schema": {
              "type": "string",
              "description": "Feature slug — propagated through the full call chain for tracking"
            },
            "required": false,
            "description": "Feature slug — propagated through the full call chain for tracking",
            "name": "x-feature-slug",
            "in": "header"
          },
          {
            "schema": {
              "type": "string",
              "description": "Explicit active goal attribution. Stored only when supplied; never inferred."
            },
            "required": false,
            "description": "Explicit active goal attribution. Stored only when supplied; never inferred.",
            "name": "x-goal",
            "in": "header"
          },
          {
            "schema": {
              "type": "string",
              "description": "Explicit brand-profile attribution. Stored only when supplied; never inferred."
            },
            "required": false,
            "description": "Explicit brand-profile attribution. Stored only when supplied; never inferred.",
            "name": "x-brand-profile-id",
            "in": "header"
          },
          {
            "schema": {
              "type": "string",
              "description": "Explicit audience attribution (human-service audience.id). Stored only when supplied; never inferred."
            },
            "required": false,
            "description": "Explicit audience attribution (human-service audience.id). Stored only when supplied; never inferred.",
            "name": "x-audience-id",
            "in": "header"
          }
        ],
        "responses": {
          "200": {
            "description": "Aggregated campaign stats",
            "content": {
              "application/json": {
                "schema": {
                  "anyOf": [
                    {
                      "$ref": "#/components/schemas/StatsResponse"
                    },
                    {
                      "$ref": "#/components/schemas/StatsGroupedResponse"
                    }
                  ]
                }
              }
            }
          },
          "400": {
            "description": "No filter provided",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Error"
                }
              }
            }
          },
          "401": {
            "description": "Unauthorized"
          },
          "500": {
            "description": "Server error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Error"
                }
              }
            }
          }
        }
      }
    },
    "/public/stats": {
      "get": {
        "summary": "Get aggregated stats (no identity headers required)",
        "description": "Same as GET /orgs/stats but without x-org-id requirement. Requires only X-API-Key. Used by leaderboard and landing pages with no user context.",
        "parameters": [
          {
            "schema": {
              "type": "string",
              "description": "Comma-separated list of run IDs"
            },
            "required": false,
            "description": "Comma-separated list of run IDs",
            "name": "runIds",
            "in": "query"
          },
          {
            "schema": {
              "type": "string",
              "description": "Filter by brand ID (matches campaigns containing this brand)"
            },
            "required": false,
            "description": "Filter by brand ID (matches campaigns containing this brand)",
            "name": "brandId",
            "in": "query"
          },
          {
            "schema": {
              "type": "string"
            },
            "required": false,
            "name": "campaignId",
            "in": "query"
          },
          {
            "schema": {
              "type": "string",
              "description": "Filter by explicit goal attribution stored in campaign metadata"
            },
            "required": false,
            "description": "Filter by explicit goal attribution stored in campaign metadata",
            "name": "goal",
            "in": "query"
          },
          {
            "schema": {
              "type": "string",
              "description": "Filter by explicit brand-profile attribution stored in campaign metadata"
            },
            "required": false,
            "description": "Filter by explicit brand-profile attribution stored in campaign metadata",
            "name": "brandProfileId",
            "in": "query"
          },
          {
            "schema": {
              "type": "string",
              "description": "Filter by explicit audience attribution stored in campaign metadata"
            },
            "required": false,
            "description": "Filter by explicit audience attribution stored in campaign metadata",
            "name": "audienceId",
            "in": "query"
          },
          {
            "schema": {
              "type": "string",
              "description": "Comma-separated list of workflow slugs to filter by"
            },
            "required": false,
            "description": "Comma-separated list of workflow slugs to filter by",
            "name": "workflowSlugs",
            "in": "query"
          },
          {
            "schema": {
              "type": "string",
              "description": "Comma-separated list of feature slugs to filter by"
            },
            "required": false,
            "description": "Comma-separated list of feature slugs to filter by",
            "name": "featureSlugs",
            "in": "query"
          },
          {
            "schema": {
              "type": "string",
              "enum": [
                "brandId",
                "campaignId",
                "workflowSlug",
                "featureSlug",
                "leadEmail",
                "audienceId",
                "day"
              ],
              "description": "Group results by dimension. groupBy=day keys buckets as YYYY-MM-DD in the requested timezone. Audience grouping uses only explicit campaign metadata."
            },
            "required": false,
            "description": "Group results by dimension. groupBy=day keys buckets as YYYY-MM-DD in the requested timezone. Audience grouping uses only explicit campaign metadata.",
            "name": "groupBy",
            "in": "query"
          },
          {
            "schema": {
              "type": "string",
              "description": "IANA timezone for groupBy=day buckets. Defaults to UTC."
            },
            "required": false,
            "description": "IANA timezone for groupBy=day buckets. Defaults to UTC.",
            "name": "timezone",
            "in": "query"
          }
        ],
        "responses": {
          "200": {
            "description": "Aggregated campaign stats",
            "content": {
              "application/json": {
                "schema": {
                  "anyOf": [
                    {
                      "$ref": "#/components/schemas/StatsResponse"
                    },
                    {
                      "$ref": "#/components/schemas/StatsGroupedResponse"
                    }
                  ]
                }
              }
            }
          },
          "400": {
            "description": "Invalid request",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Error"
                }
              }
            }
          },
          "401": {
            "description": "Unauthorized"
          },
          "500": {
            "description": "Server error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Error"
                }
              }
            }
          }
        }
      }
    },
    "/public/stats/engagement-latency": {
      "get": {
        "summary": "Get public-safe engagement latency for workflow slugs",
        "description": "Computes aggregate elapsed time from each recipient's first real send to first link click and first positive reply across the supplied workflow slugs. Returns only aggregate average, median, and sample size; no recipient, lead, campaign, or message data is exposed.",
        "parameters": [
          {
            "schema": {
              "type": "string",
              "description": "Comma-separated workflow slugs to aggregate together."
            },
            "required": true,
            "description": "Comma-separated workflow slugs to aggregate together.",
            "name": "workflowSlugs",
            "in": "query"
          }
        ],
        "responses": {
          "200": {
            "description": "Engagement latency aggregate",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/EngagementLatencyResponse"
                }
              }
            }
          },
          "400": {
            "description": "Invalid request",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Error"
                }
              }
            }
          },
          "401": {
            "description": "Unauthorized"
          },
          "500": {
            "description": "Server error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Error"
                }
              }
            }
          }
        }
      }
    },
    "/public/stats/engagement-latency/grouped": {
      "post": {
        "summary": "Get public-safe engagement latency for workflow slug groups",
        "description": "Computes aggregate elapsed time from each recipient's first real send to first link click and first positive reply for caller-owned workflow slug groups. Use group keys such as workflow dynasty slugs when the consumer owns dynasty metadata. Returns no per-recipient rows or campaign internals.",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/EngagementLatencyGroupedRequest"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "Engagement latency aggregates by caller-owned group",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/EngagementLatencyGroupedResponse"
                }
              }
            }
          },
          "400": {
            "description": "Invalid request",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Error"
                }
              }
            }
          },
          "401": {
            "description": "Unauthorized"
          },
          "500": {
            "description": "Server error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Error"
                }
              }
            }
          }
        }
      }
    },
    "/orgs/stats/grouped": {
      "post": {
        "summary": "Get stats grouped by sets of run IDs",
        "description": "Accepts named groups of run IDs and returns aggregated stats per group in a single call. Used by the leaderboard to fetch per-workflow stats.",
        "parameters": [
          {
            "schema": {
              "type": "string",
              "description": "Campaign ID — automatically injected by workflow-service on all DAG calls"
            },
            "required": false,
            "description": "Campaign ID — automatically injected by workflow-service on all DAG calls",
            "name": "x-campaign-id",
            "in": "header"
          },
          {
            "schema": {
              "type": "string",
              "description": "Brand ID(s) — comma-separated UUIDs, automatically injected by workflow-service on all DAG calls. Example: uuid1,uuid2,uuid3"
            },
            "required": false,
            "description": "Brand ID(s) — comma-separated UUIDs, automatically injected by workflow-service on all DAG calls. Example: uuid1,uuid2,uuid3",
            "name": "x-brand-id",
            "in": "header"
          },
          {
            "schema": {
              "type": "string",
              "description": "Workflow slug — automatically injected by workflow-service on all DAG calls"
            },
            "required": false,
            "description": "Workflow slug — automatically injected by workflow-service on all DAG calls",
            "name": "x-workflow-slug",
            "in": "header"
          },
          {
            "schema": {
              "type": "string",
              "description": "Feature slug — propagated through the full call chain for tracking"
            },
            "required": false,
            "description": "Feature slug — propagated through the full call chain for tracking",
            "name": "x-feature-slug",
            "in": "header"
          },
          {
            "schema": {
              "type": "string",
              "description": "Explicit active goal attribution. Stored only when supplied; never inferred."
            },
            "required": false,
            "description": "Explicit active goal attribution. Stored only when supplied; never inferred.",
            "name": "x-goal",
            "in": "header"
          },
          {
            "schema": {
              "type": "string",
              "description": "Explicit brand-profile attribution. Stored only when supplied; never inferred."
            },
            "required": false,
            "description": "Explicit brand-profile attribution. Stored only when supplied; never inferred.",
            "name": "x-brand-profile-id",
            "in": "header"
          },
          {
            "schema": {
              "type": "string",
              "description": "Explicit audience attribution (human-service audience.id). Stored only when supplied; never inferred."
            },
            "required": false,
            "description": "Explicit audience attribution (human-service audience.id). Stored only when supplied; never inferred.",
            "name": "x-audience-id",
            "in": "header"
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/GroupedStatsRequest"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "Stats per group",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/GroupedStatsResponse"
                }
              }
            }
          },
          "400": {
            "description": "Invalid request",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Error"
                }
              }
            }
          },
          "401": {
            "description": "Unauthorized"
          },
          "500": {
            "description": "Server error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Error"
                }
              }
            }
          }
        }
      }
    },
    "/internal/accounts": {
      "get": {
        "summary": "List all email accounts",
        "responses": {
          "200": {
            "description": "Accounts list"
          },
          "401": {
            "description": "Unauthorized"
          }
        }
      }
    },
    "/orgs/accounts/sync": {
      "post": {
        "summary": "Sync accounts from Instantly",
        "parameters": [
          {
            "schema": {
              "type": "string",
              "description": "Campaign ID — automatically injected by workflow-service on all DAG calls"
            },
            "required": false,
            "description": "Campaign ID — automatically injected by workflow-service on all DAG calls",
            "name": "x-campaign-id",
            "in": "header"
          },
          {
            "schema": {
              "type": "string",
              "description": "Brand ID(s) — comma-separated UUIDs, automatically injected by workflow-service on all DAG calls. Example: uuid1,uuid2,uuid3"
            },
            "required": false,
            "description": "Brand ID(s) — comma-separated UUIDs, automatically injected by workflow-service on all DAG calls. Example: uuid1,uuid2,uuid3",
            "name": "x-brand-id",
            "in": "header"
          },
          {
            "schema": {
              "type": "string",
              "description": "Workflow slug — automatically injected by workflow-service on all DAG calls"
            },
            "required": false,
            "description": "Workflow slug — automatically injected by workflow-service on all DAG calls",
            "name": "x-workflow-slug",
            "in": "header"
          },
          {
            "schema": {
              "type": "string",
              "description": "Feature slug — propagated through the full call chain for tracking"
            },
            "required": false,
            "description": "Feature slug — propagated through the full call chain for tracking",
            "name": "x-feature-slug",
            "in": "header"
          },
          {
            "schema": {
              "type": "string",
              "description": "Explicit active goal attribution. Stored only when supplied; never inferred."
            },
            "required": false,
            "description": "Explicit active goal attribution. Stored only when supplied; never inferred.",
            "name": "x-goal",
            "in": "header"
          },
          {
            "schema": {
              "type": "string",
              "description": "Explicit brand-profile attribution. Stored only when supplied; never inferred."
            },
            "required": false,
            "description": "Explicit brand-profile attribution. Stored only when supplied; never inferred.",
            "name": "x-brand-profile-id",
            "in": "header"
          },
          {
            "schema": {
              "type": "string",
              "description": "Explicit audience attribution (human-service audience.id). Stored only when supplied; never inferred."
            },
            "required": false,
            "description": "Explicit audience attribution (human-service audience.id). Stored only when supplied; never inferred.",
            "name": "x-audience-id",
            "in": "header"
          }
        ],
        "responses": {
          "200": {
            "description": "Sync complete",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/SyncResponse"
                }
              }
            }
          },
          "401": {
            "description": "Unauthorized"
          }
        }
      }
    },
    "/internal/transfer-brand": {
      "post": {
        "summary": "Transfer solo-brand rows from one org to another",
        "description": "Re-assigns org_id (and optionally brand_id) on all rows that reference exactly one brand matching sourceBrandId. When targetBrandId is present, also rewrites brand references to the target brand. Skips co-branding rows (multiple brand IDs). Idempotent.",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/TransferBrandRequest"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "Transfer complete",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/TransferBrandResponse"
                }
              }
            }
          },
          "400": {
            "description": "Invalid request",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Error"
                }
              }
            }
          },
          "401": {
            "description": "Unauthorized"
          },
          "500": {
            "description": "Server error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Error"
                }
              }
            }
          }
        }
      }
    },
    "/orgs/accounts/{email}/warmup": {
      "post": {
        "summary": "Enable or disable warmup for an account",
        "parameters": [
          {
            "schema": {
              "type": "string"
            },
            "required": true,
            "name": "email",
            "in": "path"
          },
          {
            "schema": {
              "type": "string",
              "description": "Campaign ID — automatically injected by workflow-service on all DAG calls"
            },
            "required": false,
            "description": "Campaign ID — automatically injected by workflow-service on all DAG calls",
            "name": "x-campaign-id",
            "in": "header"
          },
          {
            "schema": {
              "type": "string",
              "description": "Brand ID(s) — comma-separated UUIDs, automatically injected by workflow-service on all DAG calls. Example: uuid1,uuid2,uuid3"
            },
            "required": false,
            "description": "Brand ID(s) — comma-separated UUIDs, automatically injected by workflow-service on all DAG calls. Example: uuid1,uuid2,uuid3",
            "name": "x-brand-id",
            "in": "header"
          },
          {
            "schema": {
              "type": "string",
              "description": "Workflow slug — automatically injected by workflow-service on all DAG calls"
            },
            "required": false,
            "description": "Workflow slug — automatically injected by workflow-service on all DAG calls",
            "name": "x-workflow-slug",
            "in": "header"
          },
          {
            "schema": {
              "type": "string",
              "description": "Feature slug — propagated through the full call chain for tracking"
            },
            "required": false,
            "description": "Feature slug — propagated through the full call chain for tracking",
            "name": "x-feature-slug",
            "in": "header"
          },
          {
            "schema": {
              "type": "string",
              "description": "Explicit active goal attribution. Stored only when supplied; never inferred."
            },
            "required": false,
            "description": "Explicit active goal attribution. Stored only when supplied; never inferred.",
            "name": "x-goal",
            "in": "header"
          },
          {
            "schema": {
              "type": "string",
              "description": "Explicit brand-profile attribution. Stored only when supplied; never inferred."
            },
            "required": false,
            "description": "Explicit brand-profile attribution. Stored only when supplied; never inferred.",
            "name": "x-brand-profile-id",
            "in": "header"
          },
          {
            "schema": {
              "type": "string",
              "description": "Explicit audience attribution (human-service audience.id). Stored only when supplied; never inferred."
            },
            "required": false,
            "description": "Explicit audience attribution (human-service audience.id). Stored only when supplied; never inferred.",
            "name": "x-audience-id",
            "in": "header"
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/WarmupRequest"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "Warmup setting updated",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/WarmupResponse"
                }
              }
            }
          },
          "400": {
            "description": "Invalid request",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Error"
                }
              }
            }
          },
          "401": {
            "description": "Unauthorized"
          }
        }
      }
    },
    "/orgs/accounts/warmup-analytics": {
      "get": {
        "summary": "Get warmup analytics",
        "parameters": [
          {
            "schema": {
              "type": "string",
              "description": "Campaign ID — automatically injected by workflow-service on all DAG calls"
            },
            "required": false,
            "description": "Campaign ID — automatically injected by workflow-service on all DAG calls",
            "name": "x-campaign-id",
            "in": "header"
          },
          {
            "schema": {
              "type": "string",
              "description": "Brand ID(s) — comma-separated UUIDs, automatically injected by workflow-service on all DAG calls. Example: uuid1,uuid2,uuid3"
            },
            "required": false,
            "description": "Brand ID(s) — comma-separated UUIDs, automatically injected by workflow-service on all DAG calls. Example: uuid1,uuid2,uuid3",
            "name": "x-brand-id",
            "in": "header"
          },
          {
            "schema": {
              "type": "string",
              "description": "Workflow slug — automatically injected by workflow-service on all DAG calls"
            },
            "required": false,
            "description": "Workflow slug — automatically injected by workflow-service on all DAG calls",
            "name": "x-workflow-slug",
            "in": "header"
          },
          {
            "schema": {
              "type": "string",
              "description": "Feature slug — propagated through the full call chain for tracking"
            },
            "required": false,
            "description": "Feature slug — propagated through the full call chain for tracking",
            "name": "x-feature-slug",
            "in": "header"
          },
          {
            "schema": {
              "type": "string",
              "description": "Explicit active goal attribution. Stored only when supplied; never inferred."
            },
            "required": false,
            "description": "Explicit active goal attribution. Stored only when supplied; never inferred.",
            "name": "x-goal",
            "in": "header"
          },
          {
            "schema": {
              "type": "string",
              "description": "Explicit brand-profile attribution. Stored only when supplied; never inferred."
            },
            "required": false,
            "description": "Explicit brand-profile attribution. Stored only when supplied; never inferred.",
            "name": "x-brand-profile-id",
            "in": "header"
          },
          {
            "schema": {
              "type": "string",
              "description": "Explicit audience attribution (human-service audience.id). Stored only when supplied; never inferred."
            },
            "required": false,
            "description": "Explicit audience attribution (human-service audience.id). Stored only when supplied; never inferred.",
            "name": "x-audience-id",
            "in": "header"
          }
        ],
        "responses": {
          "200": {
            "description": "Warmup analytics"
          },
          "401": {
            "description": "Unauthorized"
          }
        }
      }
    },
    "/orgs/replies": {
      "post": {
        "summary": "Reply to a lead who replied, in their existing thread",
        "description": "Answer a prospect who wrote back, in the SAME email thread, from the SAME mailbox that originally contacted them, under the SAME persona they have been corresponding with.\n\n**The sending identity is resolved here, never supplied.** Which mailbox answers comes from `instantly_campaigns.account_email` (persisted at send time), with the mailbox recorded on the inbound message as the fallback for a historical row. The display name and signature follow that account. A caller-supplied from-address would let a reply arrive from a mailbox this prospect has never heard from — the exact failure this endpoint exists to prevent.\n\n**Threaded or nothing.** On the Instantly transport the reply goes out through `POST /emails/reply`, threaded onto the prospect's latest inbound message; on the self-send transport we dispatch it ourselves with `In-Reply-To` / `References` over the conversation we already hold in bronze. There is NO fallback to a fresh email: if the thread cannot be found the call fails with `code: \"no_reply_to_thread\"`.\n\n**Signature:** send the prospect-facing words only. This service appends the account's persona signature (idempotently — a body re-sent never stacks signatures). Deliberately NO unsubscribe footer: a one-to-one answer is not bulk mail, and the footer's `{unsubscribe_link}` merge variable only resolves on a campaign send.\n\n**Cost:** none declared. The mailbox estate is a fixed cost we absorb rather than rebill, so a reply is priced exactly like the sequence sends themselves.",
        "parameters": [
          {
            "schema": {
              "type": "string",
              "description": "Campaign ID — automatically injected by workflow-service on all DAG calls"
            },
            "required": false,
            "description": "Campaign ID — automatically injected by workflow-service on all DAG calls",
            "name": "x-campaign-id",
            "in": "header"
          },
          {
            "schema": {
              "type": "string",
              "description": "Brand ID(s) — comma-separated UUIDs, automatically injected by workflow-service on all DAG calls. Example: uuid1,uuid2,uuid3"
            },
            "required": false,
            "description": "Brand ID(s) — comma-separated UUIDs, automatically injected by workflow-service on all DAG calls. Example: uuid1,uuid2,uuid3",
            "name": "x-brand-id",
            "in": "header"
          },
          {
            "schema": {
              "type": "string",
              "description": "Workflow slug — automatically injected by workflow-service on all DAG calls"
            },
            "required": false,
            "description": "Workflow slug — automatically injected by workflow-service on all DAG calls",
            "name": "x-workflow-slug",
            "in": "header"
          },
          {
            "schema": {
              "type": "string",
              "description": "Feature slug — propagated through the full call chain for tracking"
            },
            "required": false,
            "description": "Feature slug — propagated through the full call chain for tracking",
            "name": "x-feature-slug",
            "in": "header"
          },
          {
            "schema": {
              "type": "string",
              "description": "Explicit active goal attribution. Stored only when supplied; never inferred."
            },
            "required": false,
            "description": "Explicit active goal attribution. Stored only when supplied; never inferred.",
            "name": "x-goal",
            "in": "header"
          },
          {
            "schema": {
              "type": "string",
              "description": "Explicit brand-profile attribution. Stored only when supplied; never inferred."
            },
            "required": false,
            "description": "Explicit brand-profile attribution. Stored only when supplied; never inferred.",
            "name": "x-brand-profile-id",
            "in": "header"
          },
          {
            "schema": {
              "type": "string",
              "description": "Explicit audience attribution (human-service audience.id). Stored only when supplied; never inferred."
            },
            "required": false,
            "description": "Explicit audience attribution (human-service audience.id). Stored only when supplied; never inferred.",
            "name": "x-audience-id",
            "in": "header"
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/ReplyToLeadBody"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "The reply was sent into the lead's existing thread",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ReplyToLeadResponse"
                }
              }
            }
          },
          "400": {
            "description": "Invalid body or missing x-user-id",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Error"
                }
              }
            }
          },
          "401": {
            "description": "Unauthorized"
          },
          "404": {
            "description": "No campaign in this org for the given email",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ReplyToLeadError"
                }
              }
            }
          },
          "409": {
            "description": "The reply cannot be threaded or attributed: `no_reply_to_thread`, `sending_account_unresolved`, or `mailbox_credential_unavailable`. Nothing was sent.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ReplyToLeadError"
                }
              }
            }
          },
          "502": {
            "description": "The transport refused the send (`reply_dispatch_failed`)",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ReplyToLeadError"
                }
              }
            }
          }
        }
      }
    },
    "/orgs/conversations": {
      "get": {
        "summary": "Read the messages exchanged with a lead on a campaign",
        "description": "Return what a prospect wrote and what we sent, oldest first, for one (campaign, lead) pair — the exact identity `POST /orgs/replies` takes. Intended for a worker about to answer a reply: it reads the conversation so its answer can address what that person actually said.\n\n**The whole campaign, not one stored row.** campaign-service mints a fresh campaign row every time the campaign's workflow changes and keeps the ancestors, so a campaign as the customer knows it is routinely dozens of rows and a prospect emailed over months sits in several. The answer spans every row of the campaign that holds this lead, merged into one ordered thread — so its earliest outbound message is the send the delivery evidence reports as first. `campaignIds` names the rows it drew from and each message carries its own; a single-row campaign answers as it always did.\n\n**Both transports.** The consumer cannot know which pipe carried a given prospect, exactly as the reply endpoint cannot. On the Instantly transport the messages come from Instantly's Unibox; on the self-send transport they are interleaved from what we dispatched and what we read back over IMAP. One response shape either way.\n\n**Text, not HTML.** Every `text` is markup-stripped so it drops straight into a prompt. No truncation is applied here; note that a self-send inbound body is stored as the first 4000 characters of the message at ingestion time.\n\n**Absent is not empty.** A conversation this org has no record of is a 404 (`campaign_not_found`); an exchange that exists with nothing said is a 200 with an empty `messages`; one we hold but cannot read is a 502 (`thread_unavailable`, or `campaign_identity_unavailable` when campaign-service could not say which rows the campaign is made of). No path returns an empty list to stand in for a failure.\n\n**Cost:** none. It sends nothing and declares nothing — a read of what already happened.",
        "parameters": [
          {
            "schema": {
              "type": "string",
              "minLength": 1,
              "description": "Logical campaign id — the same key POST /orgs/replies takes"
            },
            "required": true,
            "description": "Logical campaign id — the same key POST /orgs/replies takes",
            "name": "campaign_id",
            "in": "query"
          },
          {
            "schema": {
              "type": "string",
              "format": "email",
              "description": "The lead whose conversation to read"
            },
            "required": true,
            "description": "The lead whose conversation to read",
            "name": "email",
            "in": "query"
          },
          {
            "schema": {
              "type": "string",
              "description": "Campaign ID — automatically injected by workflow-service on all DAG calls"
            },
            "required": false,
            "description": "Campaign ID — automatically injected by workflow-service on all DAG calls",
            "name": "x-campaign-id",
            "in": "header"
          },
          {
            "schema": {
              "type": "string",
              "description": "Brand ID(s) — comma-separated UUIDs, automatically injected by workflow-service on all DAG calls. Example: uuid1,uuid2,uuid3"
            },
            "required": false,
            "description": "Brand ID(s) — comma-separated UUIDs, automatically injected by workflow-service on all DAG calls. Example: uuid1,uuid2,uuid3",
            "name": "x-brand-id",
            "in": "header"
          },
          {
            "schema": {
              "type": "string",
              "description": "Workflow slug — automatically injected by workflow-service on all DAG calls"
            },
            "required": false,
            "description": "Workflow slug — automatically injected by workflow-service on all DAG calls",
            "name": "x-workflow-slug",
            "in": "header"
          },
          {
            "schema": {
              "type": "string",
              "description": "Feature slug — propagated through the full call chain for tracking"
            },
            "required": false,
            "description": "Feature slug — propagated through the full call chain for tracking",
            "name": "x-feature-slug",
            "in": "header"
          },
          {
            "schema": {
              "type": "string",
              "description": "Explicit active goal attribution. Stored only when supplied; never inferred."
            },
            "required": false,
            "description": "Explicit active goal attribution. Stored only when supplied; never inferred.",
            "name": "x-goal",
            "in": "header"
          },
          {
            "schema": {
              "type": "string",
              "description": "Explicit brand-profile attribution. Stored only when supplied; never inferred."
            },
            "required": false,
            "description": "Explicit brand-profile attribution. Stored only when supplied; never inferred.",
            "name": "x-brand-profile-id",
            "in": "header"
          },
          {
            "schema": {
              "type": "string",
              "description": "Explicit audience attribution (human-service audience.id). Stored only when supplied; never inferred."
            },
            "required": false,
            "description": "Explicit audience attribution (human-service audience.id). Stored only when supplied; never inferred.",
            "name": "x-audience-id",
            "in": "header"
          }
        ],
        "responses": {
          "200": {
            "description": "The conversation, oldest first (possibly empty)",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/LeadConversationResponse"
                }
              }
            }
          },
          "400": {
            "description": "Invalid query or missing x-user-id",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Error"
                }
              }
            }
          },
          "401": {
            "description": "Unauthorized"
          },
          "404": {
            "description": "No campaign in this org for the given email (`campaign_not_found`)",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/LeadConversationError"
                }
              }
            }
          },
          "502": {
            "description": "The sequence exists but its thread could not be read (`thread_unavailable`)",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/LeadConversationError"
                }
              }
            }
          }
        }
      }
    },
    "/orgs/engaged-leads": {
      "get": {
        "summary": "The org's leads that have shown interest",
        "description": "List the leads worth opening a conversation on: everyone who replied without asking to stop, plus everyone who clicked a link we sent. Each row carries the exact identity `GET /orgs/conversations` takes, so a dashboard can list the panel and then read each thread.\n\n**Why this exists.** `GET /orgs/conversations` answers about ONE (campaign, lead) pair the caller must already know. A consumer holding a lead has no way to discover which of an org's tens of thousands of contacted leads ever engaged, nor which campaign id to ask about. This is the discovery half.\n\n**The gate.** `(replied AND NOT unsubscribed) OR clicked`. Any human reply counts whatever its sentiment — a negative reply is still a conversation worth reading, and a 'not right now' is recyclable pipeline. An unsubscribe request is the one answer that is explicitly a request to stop, so it is excluded. Autoresponders never appear: `auto_reply_received` and `lead_out_of_office` are distinct event types that never set `replied`.\n\n**⚠️ `clicked` is a click on a link WE sent**, on either transport — it is the only visit signal this service has. An anonymous website visit that never went through one of our links is invisible here.\n\n**No `engagementKind` scalar, deliberately.** A lead who clicked on Monday and replied on Friday has one engagement start and two signals; one enum would force a label that disagrees with its own timestamp. `engagedAt` is when interest started, the two booleans say which signals exist, and the two timestamps say when each happened.\n\n**Read from gold**, the same projection `POST /orgs/status` answers from — so `replyKind` and `disqualified` cannot disagree between the two surfaces.\n\n**Cost:** none. It sends nothing and declares nothing.",
        "parameters": [
          {
            "schema": {
              "type": "string",
              "format": "uuid",
              "description": "Only leads whose sequence carries this brand"
            },
            "required": false,
            "description": "Only leads whose sequence carries this brand",
            "name": "brand_id",
            "in": "query"
          },
          {
            "schema": {
              "type": "string",
              "minLength": 1,
              "description": "Only leads on this logical campaign"
            },
            "required": false,
            "description": "Only leads on this logical campaign",
            "name": "campaign_id",
            "in": "query"
          },
          {
            "schema": {
              "type": "string",
              "format": "date-time",
              "description": "Only leads whose engagement STARTED at or after this instant"
            },
            "required": false,
            "description": "Only leads whose engagement STARTED at or after this instant",
            "name": "since",
            "in": "query"
          },
          {
            "schema": {
              "type": "integer",
              "minimum": 0,
              "exclusiveMinimum": true,
              "description": "Cap the number of rows. Omitted, every engaged lead is returned — the population is a few hundred per org at the largest, so a silent default would hide the tail."
            },
            "required": false,
            "description": "Cap the number of rows. Omitted, every engaged lead is returned — the population is a few hundred per org at the largest, so a silent default would hide the tail.",
            "name": "limit",
            "in": "query"
          },
          {
            "schema": {
              "type": "string",
              "description": "Campaign ID — automatically injected by workflow-service on all DAG calls"
            },
            "required": false,
            "description": "Campaign ID — automatically injected by workflow-service on all DAG calls",
            "name": "x-campaign-id",
            "in": "header"
          },
          {
            "schema": {
              "type": "string",
              "description": "Brand ID(s) — comma-separated UUIDs, automatically injected by workflow-service on all DAG calls. Example: uuid1,uuid2,uuid3"
            },
            "required": false,
            "description": "Brand ID(s) — comma-separated UUIDs, automatically injected by workflow-service on all DAG calls. Example: uuid1,uuid2,uuid3",
            "name": "x-brand-id",
            "in": "header"
          },
          {
            "schema": {
              "type": "string",
              "description": "Workflow slug — automatically injected by workflow-service on all DAG calls"
            },
            "required": false,
            "description": "Workflow slug — automatically injected by workflow-service on all DAG calls",
            "name": "x-workflow-slug",
            "in": "header"
          },
          {
            "schema": {
              "type": "string",
              "description": "Feature slug — propagated through the full call chain for tracking"
            },
            "required": false,
            "description": "Feature slug — propagated through the full call chain for tracking",
            "name": "x-feature-slug",
            "in": "header"
          },
          {
            "schema": {
              "type": "string",
              "description": "Explicit active goal attribution. Stored only when supplied; never inferred."
            },
            "required": false,
            "description": "Explicit active goal attribution. Stored only when supplied; never inferred.",
            "name": "x-goal",
            "in": "header"
          },
          {
            "schema": {
              "type": "string",
              "description": "Explicit brand-profile attribution. Stored only when supplied; never inferred."
            },
            "required": false,
            "description": "Explicit brand-profile attribution. Stored only when supplied; never inferred.",
            "name": "x-brand-profile-id",
            "in": "header"
          },
          {
            "schema": {
              "type": "string",
              "description": "Explicit audience attribution (human-service audience.id). Stored only when supplied; never inferred."
            },
            "required": false,
            "description": "Explicit audience attribution (human-service audience.id). Stored only when supplied; never inferred.",
            "name": "x-audience-id",
            "in": "header"
          }
        ],
        "responses": {
          "200": {
            "description": "The engaged leads, most recently engaged first (possibly empty)",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/EngagedLeadsResponse"
                }
              }
            }
          },
          "400": {
            "description": "Invalid query",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Error"
                }
              }
            }
          },
          "401": {
            "description": "Unauthorized"
          },
          "500": {
            "description": "The engaged leads could not be read",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Error"
                }
              }
            }
          }
        }
      }
    },
    "/orgs/manual-qualifications": {
      "post": {
        "summary": "Set a manual reply qualification for a (campaign, lead) pair",
        "description": "Record a human-set reply classification for a lead in a campaign. Used when Instantly's automatic webhook reply classification fails to detect a reply (e.g. the reply was sent to a non-leurre account that Instantly does not monitor).\n\n**Bronze:** an `instantly_manual_qualifications_raw` row is appended for audit (append-only).\n\n**Silver / Gold:** a corresponding row is inserted into `instantly_events` with `source='manual'`, so analytics counters (RepliesDetail) include the manual qualification alongside webhook events. `instantly_campaigns.reply_classification` is updated to the derived positive/negative/neutral value and `reply_classification_source` is set to `manual` so subsequent webhook events do not overwrite the human choice.\n\n**Idempotence:** if the STANDING statement for (org, campaign, lead) already has `status`, the call is a no-op — no new bronze row, no side effects. The response includes `idempotent: true` and the existing row. A withdrawn statement does not stand, so re-stating the same kind after a withdrawal records it again.",
        "parameters": [
          {
            "schema": {
              "type": "string",
              "description": "Campaign ID — automatically injected by workflow-service on all DAG calls"
            },
            "required": false,
            "description": "Campaign ID — automatically injected by workflow-service on all DAG calls",
            "name": "x-campaign-id",
            "in": "header"
          },
          {
            "schema": {
              "type": "string",
              "description": "Brand ID(s) — comma-separated UUIDs, automatically injected by workflow-service on all DAG calls. Example: uuid1,uuid2,uuid3"
            },
            "required": false,
            "description": "Brand ID(s) — comma-separated UUIDs, automatically injected by workflow-service on all DAG calls. Example: uuid1,uuid2,uuid3",
            "name": "x-brand-id",
            "in": "header"
          },
          {
            "schema": {
              "type": "string",
              "description": "Workflow slug — automatically injected by workflow-service on all DAG calls"
            },
            "required": false,
            "description": "Workflow slug — automatically injected by workflow-service on all DAG calls",
            "name": "x-workflow-slug",
            "in": "header"
          },
          {
            "schema": {
              "type": "string",
              "description": "Feature slug — propagated through the full call chain for tracking"
            },
            "required": false,
            "description": "Feature slug — propagated through the full call chain for tracking",
            "name": "x-feature-slug",
            "in": "header"
          },
          {
            "schema": {
              "type": "string",
              "description": "Explicit active goal attribution. Stored only when supplied; never inferred."
            },
            "required": false,
            "description": "Explicit active goal attribution. Stored only when supplied; never inferred.",
            "name": "x-goal",
            "in": "header"
          },
          {
            "schema": {
              "type": "string",
              "description": "Explicit brand-profile attribution. Stored only when supplied; never inferred."
            },
            "required": false,
            "description": "Explicit brand-profile attribution. Stored only when supplied; never inferred.",
            "name": "x-brand-profile-id",
            "in": "header"
          },
          {
            "schema": {
              "type": "string",
              "description": "Explicit audience attribution (human-service audience.id). Stored only when supplied; never inferred."
            },
            "required": false,
            "description": "Explicit audience attribution (human-service audience.id). Stored only when supplied; never inferred.",
            "name": "x-audience-id",
            "in": "header"
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/ManualQualificationCreateBody"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "Manual qualification recorded (or idempotent no-op)",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ManualQualificationCreateResponse"
                }
              }
            }
          },
          "400": {
            "description": "Invalid body or missing identity header",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Error"
                }
              }
            }
          },
          "401": {
            "description": "Unauthorized"
          },
          "404": {
            "description": "Campaign not found in this org for the given email",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Error"
                }
              }
            }
          }
        }
      },
      "get": {
        "summary": "List manual reply qualifications (org-scoped audit history)",
        "description": "Returns the org's manual qualification history, sorted by `qualifiedAt` DESC. Optionally filter by `campaign_id` and/or `email`. Cross-org reads are blocked — only rows where `org_id` matches the request header are returned.",
        "parameters": [
          {
            "schema": {
              "type": "string",
              "minLength": 1,
              "description": "Filter by logical campaign id"
            },
            "required": false,
            "description": "Filter by logical campaign id",
            "name": "campaign_id",
            "in": "query"
          },
          {
            "schema": {
              "type": "string",
              "format": "email",
              "description": "Filter by lead email"
            },
            "required": false,
            "description": "Filter by lead email",
            "name": "email",
            "in": "query"
          },
          {
            "schema": {
              "type": "integer",
              "minimum": 1,
              "maximum": 500,
              "description": "Max rows to return (default 200, max 500)"
            },
            "required": false,
            "description": "Max rows to return (default 200, max 500)",
            "name": "limit",
            "in": "query"
          },
          {
            "schema": {
              "type": "string",
              "description": "Campaign ID — automatically injected by workflow-service on all DAG calls"
            },
            "required": false,
            "description": "Campaign ID — automatically injected by workflow-service on all DAG calls",
            "name": "x-campaign-id",
            "in": "header"
          },
          {
            "schema": {
              "type": "string",
              "description": "Brand ID(s) — comma-separated UUIDs, automatically injected by workflow-service on all DAG calls. Example: uuid1,uuid2,uuid3"
            },
            "required": false,
            "description": "Brand ID(s) — comma-separated UUIDs, automatically injected by workflow-service on all DAG calls. Example: uuid1,uuid2,uuid3",
            "name": "x-brand-id",
            "in": "header"
          },
          {
            "schema": {
              "type": "string",
              "description": "Workflow slug — automatically injected by workflow-service on all DAG calls"
            },
            "required": false,
            "description": "Workflow slug — automatically injected by workflow-service on all DAG calls",
            "name": "x-workflow-slug",
            "in": "header"
          },
          {
            "schema": {
              "type": "string",
              "description": "Feature slug — propagated through the full call chain for tracking"
            },
            "required": false,
            "description": "Feature slug — propagated through the full call chain for tracking",
            "name": "x-feature-slug",
            "in": "header"
          },
          {
            "schema": {
              "type": "string",
              "description": "Explicit active goal attribution. Stored only when supplied; never inferred."
            },
            "required": false,
            "description": "Explicit active goal attribution. Stored only when supplied; never inferred.",
            "name": "x-goal",
            "in": "header"
          },
          {
            "schema": {
              "type": "string",
              "description": "Explicit brand-profile attribution. Stored only when supplied; never inferred."
            },
            "required": false,
            "description": "Explicit brand-profile attribution. Stored only when supplied; never inferred.",
            "name": "x-brand-profile-id",
            "in": "header"
          },
          {
            "schema": {
              "type": "string",
              "description": "Explicit audience attribution (human-service audience.id). Stored only when supplied; never inferred."
            },
            "required": false,
            "description": "Explicit audience attribution (human-service audience.id). Stored only when supplied; never inferred.",
            "name": "x-audience-id",
            "in": "header"
          }
        ],
        "responses": {
          "200": {
            "description": "List of manual qualifications",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ManualQualificationListResponse"
                }
              }
            }
          },
          "400": {
            "description": "Invalid query parameters",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Error"
                }
              }
            }
          },
          "401": {
            "description": "Unauthorized"
          }
        }
      }
    },
    "/orgs/manual-qualifications/withdrawals": {
      "post": {
        "summary": "Withdraw the standing manual reply qualification for a (campaign, lead) pair",
        "description": "Take back a human statement about a reply — for a person who picked the wrong kind by mistake. After the withdrawal the lead reads as it did before anybody said anything: no standing human statement, and the AUTOMATIC classification takes over again.\n\n**A correction, not an erasure.** Nothing is deleted and no \"none\" value is added to the reply-kind vocabulary. The statement row stays byte-identical in `instantly_manual_qualifications_raw` and a row is APPENDED to `instantly_manual_qualification_withdrawals` recording that it no longer stands, by whom and when. `GET /orgs/manual-qualifications` returns both, and a withdrawn one carries a non-null `withdrawnAt`.\n\n**Silver / Gold:** the statement's silver mirror event is marked withdrawn (the row is kept — it is the audit of what was asserted) so the current-sentiment projection stops counting it; `instantly_campaigns.reply_classification` is recomputed from whatever automatic classification is left (NULL when there is none) and `reply_classification_source` returns to `auto`, which releases the pin so a subsequent webhook event is free to classify the reply as it normally would.\n\n**Scope:** this withdraws the reply KIND and its pin. It does not retract the separate fact that a reply arrived, nor undo the sequence stop, hold cancellation and Instantly pause a sequence-stopping statement already caused — those are irreversible actions already taken. The resulting state (a reply on record with no kind attached) is exactly the state of an auto-detected reply nobody has qualified.\n\n**Idempotence:** withdrawing when nothing is standing writes nothing and returns 404 with `code: \"no_standing_qualification\"` — including a second withdrawal of the same statement.",
        "parameters": [
          {
            "schema": {
              "type": "string",
              "description": "Campaign ID — automatically injected by workflow-service on all DAG calls"
            },
            "required": false,
            "description": "Campaign ID — automatically injected by workflow-service on all DAG calls",
            "name": "x-campaign-id",
            "in": "header"
          },
          {
            "schema": {
              "type": "string",
              "description": "Brand ID(s) — comma-separated UUIDs, automatically injected by workflow-service on all DAG calls. Example: uuid1,uuid2,uuid3"
            },
            "required": false,
            "description": "Brand ID(s) — comma-separated UUIDs, automatically injected by workflow-service on all DAG calls. Example: uuid1,uuid2,uuid3",
            "name": "x-brand-id",
            "in": "header"
          },
          {
            "schema": {
              "type": "string",
              "description": "Workflow slug — automatically injected by workflow-service on all DAG calls"
            },
            "required": false,
            "description": "Workflow slug — automatically injected by workflow-service on all DAG calls",
            "name": "x-workflow-slug",
            "in": "header"
          },
          {
            "schema": {
              "type": "string",
              "description": "Feature slug — propagated through the full call chain for tracking"
            },
            "required": false,
            "description": "Feature slug — propagated through the full call chain for tracking",
            "name": "x-feature-slug",
            "in": "header"
          },
          {
            "schema": {
              "type": "string",
              "description": "Explicit active goal attribution. Stored only when supplied; never inferred."
            },
            "required": false,
            "description": "Explicit active goal attribution. Stored only when supplied; never inferred.",
            "name": "x-goal",
            "in": "header"
          },
          {
            "schema": {
              "type": "string",
              "description": "Explicit brand-profile attribution. Stored only when supplied; never inferred."
            },
            "required": false,
            "description": "Explicit brand-profile attribution. Stored only when supplied; never inferred.",
            "name": "x-brand-profile-id",
            "in": "header"
          },
          {
            "schema": {
              "type": "string",
              "description": "Explicit audience attribution (human-service audience.id). Stored only when supplied; never inferred."
            },
            "required": false,
            "description": "Explicit audience attribution (human-service audience.id). Stored only when supplied; never inferred.",
            "name": "x-audience-id",
            "in": "header"
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/ManualQualificationWithdrawBody"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "The standing statement was withdrawn",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ManualQualificationWithdrawResponse"
                }
              }
            }
          },
          "400": {
            "description": "Invalid body or missing identity header",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Error"
                }
              }
            }
          },
          "401": {
            "description": "Unauthorized"
          },
          "404": {
            "description": "Nothing to withdraw. `code: \"no_standing_qualification\"` — nobody has stated a reply kind for this pair, or the statement was already withdrawn. `code: \"campaign_not_found\"` — no campaign in this org for the given email.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ManualQualificationWithdrawError"
                }
              }
            }
          }
        }
      }
    },
    "/orgs/status": {
      "post": {
        "summary": "Batch delivery status check for emails",
        "description": "Batch delivery status check. Filters are in the body — headers are tracing/logging only.\n\n**Modes:**\n- **Brand mode** (`brandId` set, no `campaignId`): returns `byCampaign` (per-campaign breakdown keyed by campaign UUID), `brand` (aggregated), and `global`.\n- **Campaign mode** (`campaignId` set, with or without `brandId`): returns `campaign` (single campaign status) and `global`. When both are provided, `brandId` is ignored.\n- **Global only** (neither): returns only `global`.\n\n**Aggregation rules for `brand`:**\n- Boolean fields (`contacted`, `sent`, `delivered`, `opened`, `clicked`, `replied`, `bounced`, `unsubscribed`): `true` if true in at least one campaign (BOOL_OR).\n- `replyClassification`: from the campaign with the most recent `lastDeliveredAt` that has a non-null classification.\n- `lastDeliveredAt`: MAX across all campaigns.\n- `firstContactedAt` / `firstSentAt` / `firstDeliveredAt` / `firstOpenedAt` / `firstClickedAt` / `firstRepliedAt` / `firstBouncedAt` / `firstUnsubscribedAt`: first-occurrence (MIN) timestamp of each event type in the scope, null if it never happened; brand = MIN across campaigns. Each agrees with its boolean (non-null iff the boolean is true; `firstDeliveredAt` consistent with `delivered = sent AND NOT bounced`).\n\n**`global.email`** aggregates `bounced`/`unsubscribed` across ALL campaigns in the org, regardless of brand or campaign filters.\n\nFields not applicable to the active mode are always present but set to `null`.",
        "parameters": [
          {
            "schema": {
              "type": "string",
              "description": "Campaign ID — automatically injected by workflow-service on all DAG calls"
            },
            "required": false,
            "description": "Campaign ID — automatically injected by workflow-service on all DAG calls",
            "name": "x-campaign-id",
            "in": "header"
          },
          {
            "schema": {
              "type": "string",
              "description": "Brand ID(s) — comma-separated UUIDs, automatically injected by workflow-service on all DAG calls. Example: uuid1,uuid2,uuid3"
            },
            "required": false,
            "description": "Brand ID(s) — comma-separated UUIDs, automatically injected by workflow-service on all DAG calls. Example: uuid1,uuid2,uuid3",
            "name": "x-brand-id",
            "in": "header"
          },
          {
            "schema": {
              "type": "string",
              "description": "Workflow slug — automatically injected by workflow-service on all DAG calls"
            },
            "required": false,
            "description": "Workflow slug — automatically injected by workflow-service on all DAG calls",
            "name": "x-workflow-slug",
            "in": "header"
          },
          {
            "schema": {
              "type": "string",
              "description": "Feature slug — propagated through the full call chain for tracking"
            },
            "required": false,
            "description": "Feature slug — propagated through the full call chain for tracking",
            "name": "x-feature-slug",
            "in": "header"
          },
          {
            "schema": {
              "type": "string",
              "description": "Explicit active goal attribution. Stored only when supplied; never inferred."
            },
            "required": false,
            "description": "Explicit active goal attribution. Stored only when supplied; never inferred.",
            "name": "x-goal",
            "in": "header"
          },
          {
            "schema": {
              "type": "string",
              "description": "Explicit brand-profile attribution. Stored only when supplied; never inferred."
            },
            "required": false,
            "description": "Explicit brand-profile attribution. Stored only when supplied; never inferred.",
            "name": "x-brand-profile-id",
            "in": "header"
          },
          {
            "schema": {
              "type": "string",
              "description": "Explicit audience attribution (human-service audience.id). Stored only when supplied; never inferred."
            },
            "required": false,
            "description": "Explicit audience attribution (human-service audience.id). Stored only when supplied; never inferred.",
            "name": "x-audience-id",
            "in": "header"
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/StatusRequest"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "Delivery status results",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/StatusResponse"
                }
              }
            }
          },
          "400": {
            "description": "Invalid request",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Error"
                }
              }
            }
          },
          "401": {
            "description": "Unauthorized"
          },
          "500": {
            "description": "Server error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Error"
                }
              }
            }
          }
        }
      }
    },
    "/orgs/opt-outs": {
      "post": {
        "summary": "Record that a person asked a human to stop contacting them",
        "description": "A prospect rarely clicks the unsubscribe link: they send an SMS, they call, they reply to a thread somebody forwarded them, they say it in person. This records that statement — never infers it — and then honours it.\n\n**It stops the sending.** For every campaign this org holds for that address, a real `lead_unsubscribed` silver event is promoted through the SAME path a clicked unsubscribe uses (the sequence stops, the remaining provisioned holds are cancelled) AND the campaign is paused at the sender. The pause is the half the click path gets for free from Instantly and this path cannot: Instantly never saw the SMS.\n\n**It surfaces where a clicked unsubscribe surfaces.** `POST /orgs/status` reports `unsubscribed: true` for that person, scoped and global, with no second field for a consumer to learn.\n\n**Scope is the PERSON, not a campaign.** Honouring \"stop contacting me\" in one campaign while another keeps sending is the outcome the law cares about.\n\n**Bronze:** an `instantly_lead_optouts_raw` row is appended (append-only) carrying who stated it, when, and through which channel — it is a consent record.\n\n**Idempotence:** a standing record for the same person returns `idempotent: true` with no second row and no repeated side effects. The record is written even when the org holds no campaign for the address; the response then reports zero campaigns.",
        "parameters": [
          {
            "schema": {
              "type": "string",
              "description": "Campaign ID — automatically injected by workflow-service on all DAG calls"
            },
            "required": false,
            "description": "Campaign ID — automatically injected by workflow-service on all DAG calls",
            "name": "x-campaign-id",
            "in": "header"
          },
          {
            "schema": {
              "type": "string",
              "description": "Brand ID(s) — comma-separated UUIDs, automatically injected by workflow-service on all DAG calls. Example: uuid1,uuid2,uuid3"
            },
            "required": false,
            "description": "Brand ID(s) — comma-separated UUIDs, automatically injected by workflow-service on all DAG calls. Example: uuid1,uuid2,uuid3",
            "name": "x-brand-id",
            "in": "header"
          },
          {
            "schema": {
              "type": "string",
              "description": "Workflow slug — automatically injected by workflow-service on all DAG calls"
            },
            "required": false,
            "description": "Workflow slug — automatically injected by workflow-service on all DAG calls",
            "name": "x-workflow-slug",
            "in": "header"
          },
          {
            "schema": {
              "type": "string",
              "description": "Feature slug — propagated through the full call chain for tracking"
            },
            "required": false,
            "description": "Feature slug — propagated through the full call chain for tracking",
            "name": "x-feature-slug",
            "in": "header"
          },
          {
            "schema": {
              "type": "string",
              "description": "Explicit active goal attribution. Stored only when supplied; never inferred."
            },
            "required": false,
            "description": "Explicit active goal attribution. Stored only when supplied; never inferred.",
            "name": "x-goal",
            "in": "header"
          },
          {
            "schema": {
              "type": "string",
              "description": "Explicit brand-profile attribution. Stored only when supplied; never inferred."
            },
            "required": false,
            "description": "Explicit brand-profile attribution. Stored only when supplied; never inferred.",
            "name": "x-brand-profile-id",
            "in": "header"
          },
          {
            "schema": {
              "type": "string",
              "description": "Explicit audience attribution (human-service audience.id). Stored only when supplied; never inferred."
            },
            "required": false,
            "description": "Explicit audience attribution (human-service audience.id). Stored only when supplied; never inferred.",
            "name": "x-audience-id",
            "in": "header"
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/LeadOptOutCreateBody"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "Opt-out recorded (or idempotent no-op)",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/LeadOptOutCreateResponse"
                }
              }
            }
          },
          "400": {
            "description": "Invalid body or missing identity header",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Error"
                }
              }
            }
          },
          "401": {
            "description": "Unauthorized"
          }
        }
      },
      "get": {
        "summary": "The org's recorded opt-out log",
        "description": "Every opt-out recorded by a human for this org, newest first. Withdrawn records are returned too and carry `withdrawnAt` / `withdrawnBy` — hiding them would destroy the audit. Pass `standing_only=true` for only the records that still stand.",
        "parameters": [
          {
            "schema": {
              "type": "string",
              "format": "email",
              "description": "Filter by lead email"
            },
            "required": false,
            "description": "Filter by lead email",
            "name": "email",
            "in": "query"
          },
          {
            "schema": {
              "type": "boolean",
              "nullable": true,
              "description": "Return only records that still STAND (default false — withdrawn records are part of the audit)"
            },
            "required": false,
            "description": "Return only records that still STAND (default false — withdrawn records are part of the audit)",
            "name": "standing_only",
            "in": "query"
          },
          {
            "schema": {
              "type": "integer",
              "minimum": 1,
              "maximum": 500,
              "description": "Max rows to return (default 200, max 500)"
            },
            "required": false,
            "description": "Max rows to return (default 200, max 500)",
            "name": "limit",
            "in": "query"
          },
          {
            "schema": {
              "type": "string",
              "description": "Campaign ID — automatically injected by workflow-service on all DAG calls"
            },
            "required": false,
            "description": "Campaign ID — automatically injected by workflow-service on all DAG calls",
            "name": "x-campaign-id",
            "in": "header"
          },
          {
            "schema": {
              "type": "string",
              "description": "Brand ID(s) — comma-separated UUIDs, automatically injected by workflow-service on all DAG calls. Example: uuid1,uuid2,uuid3"
            },
            "required": false,
            "description": "Brand ID(s) — comma-separated UUIDs, automatically injected by workflow-service on all DAG calls. Example: uuid1,uuid2,uuid3",
            "name": "x-brand-id",
            "in": "header"
          },
          {
            "schema": {
              "type": "string",
              "description": "Workflow slug — automatically injected by workflow-service on all DAG calls"
            },
            "required": false,
            "description": "Workflow slug — automatically injected by workflow-service on all DAG calls",
            "name": "x-workflow-slug",
            "in": "header"
          },
          {
            "schema": {
              "type": "string",
              "description": "Feature slug — propagated through the full call chain for tracking"
            },
            "required": false,
            "description": "Feature slug — propagated through the full call chain for tracking",
            "name": "x-feature-slug",
            "in": "header"
          },
          {
            "schema": {
              "type": "string",
              "description": "Explicit active goal attribution. Stored only when supplied; never inferred."
            },
            "required": false,
            "description": "Explicit active goal attribution. Stored only when supplied; never inferred.",
            "name": "x-goal",
            "in": "header"
          },
          {
            "schema": {
              "type": "string",
              "description": "Explicit brand-profile attribution. Stored only when supplied; never inferred."
            },
            "required": false,
            "description": "Explicit brand-profile attribution. Stored only when supplied; never inferred.",
            "name": "x-brand-profile-id",
            "in": "header"
          },
          {
            "schema": {
              "type": "string",
              "description": "Explicit audience attribution (human-service audience.id). Stored only when supplied; never inferred."
            },
            "required": false,
            "description": "Explicit audience attribution (human-service audience.id). Stored only when supplied; never inferred.",
            "name": "x-audience-id",
            "in": "header"
          }
        ],
        "responses": {
          "200": {
            "description": "Recorded opt-outs",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/LeadOptOutListResponse"
                }
              }
            }
          },
          "400": {
            "description": "Invalid query",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Error"
                }
              }
            }
          },
          "401": {
            "description": "Unauthorized"
          }
        }
      }
    },
    "/orgs/opt-outs/withdrawals": {
      "post": {
        "summary": "Withdraw the standing recorded opt-out for a person",
        "description": "Take a recorded opt-out back — recorded on the wrong lead, or a prospect who came back and asked to hear from us again. After it, `POST /orgs/status` stops reporting that person as unsubscribed.\n\n**A correction, not an erasure.** The record stays byte-identical in `instantly_lead_optouts_raw` and a row is APPENDED to `instantly_lead_optout_withdrawals`. `GET /orgs/opt-outs` returns both; a withdrawn one carries a non-null `withdrawnAt`.\n\n**Silver / Gold:** only the `lead_unsubscribed` events THIS record promoted are marked withdrawn (the rows are kept — silver is the audit of what was asserted), and the gold status row is refreshed. A `lead_unsubscribed` the prospect produced by clicking the link is never touched: nobody withdrew that.\n\n**Scope:** this releases the OPT-OUT. It does not resume the sequences it stopped — the holds were cancelled and the campaigns paused, and silently restarting outreach at somebody who asked us to stop is the one mistake worth being unable to make by accident.\n\n**Idempotence:** withdrawing when nothing stands writes nothing and returns 404 `no_standing_optout`, including a second withdrawal of the same record.",
        "parameters": [
          {
            "schema": {
              "type": "string",
              "description": "Campaign ID — automatically injected by workflow-service on all DAG calls"
            },
            "required": false,
            "description": "Campaign ID — automatically injected by workflow-service on all DAG calls",
            "name": "x-campaign-id",
            "in": "header"
          },
          {
            "schema": {
              "type": "string",
              "description": "Brand ID(s) — comma-separated UUIDs, automatically injected by workflow-service on all DAG calls. Example: uuid1,uuid2,uuid3"
            },
            "required": false,
            "description": "Brand ID(s) — comma-separated UUIDs, automatically injected by workflow-service on all DAG calls. Example: uuid1,uuid2,uuid3",
            "name": "x-brand-id",
            "in": "header"
          },
          {
            "schema": {
              "type": "string",
              "description": "Workflow slug — automatically injected by workflow-service on all DAG calls"
            },
            "required": false,
            "description": "Workflow slug — automatically injected by workflow-service on all DAG calls",
            "name": "x-workflow-slug",
            "in": "header"
          },
          {
            "schema": {
              "type": "string",
              "description": "Feature slug — propagated through the full call chain for tracking"
            },
            "required": false,
            "description": "Feature slug — propagated through the full call chain for tracking",
            "name": "x-feature-slug",
            "in": "header"
          },
          {
            "schema": {
              "type": "string",
              "description": "Explicit active goal attribution. Stored only when supplied; never inferred."
            },
            "required": false,
            "description": "Explicit active goal attribution. Stored only when supplied; never inferred.",
            "name": "x-goal",
            "in": "header"
          },
          {
            "schema": {
              "type": "string",
              "description": "Explicit brand-profile attribution. Stored only when supplied; never inferred."
            },
            "required": false,
            "description": "Explicit brand-profile attribution. Stored only when supplied; never inferred.",
            "name": "x-brand-profile-id",
            "in": "header"
          },
          {
            "schema": {
              "type": "string",
              "description": "Explicit audience attribution (human-service audience.id). Stored only when supplied; never inferred."
            },
            "required": false,
            "description": "Explicit audience attribution (human-service audience.id). Stored only when supplied; never inferred.",
            "name": "x-audience-id",
            "in": "header"
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/LeadOptOutWithdrawBody"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "The standing opt-out was withdrawn",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/LeadOptOutWithdrawResponse"
                }
              }
            }
          },
          "400": {
            "description": "Invalid body or missing identity header",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Error"
                }
              }
            }
          },
          "401": {
            "description": "Unauthorized"
          },
          "404": {
            "description": "Nothing stands for this lead",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/LeadOptOutWithdrawError"
                }
              }
            }
          }
        }
      }
    },
    "/internal/audit/sending-forecast": {
      "get": {
        "summary": "Fleet sending forecast — daily capacity vs upcoming scheduled volume",
        "description": "Platform-scoped (no org). Returns the cold-email fleet's available daily sending CAPACITY (sum of the daily send limit over only healthy accounts) alongside a TRUE per-day projection of upcoming scheduled send VOLUME (active campaigns' remaining un-sent sequence steps bucketed on their raw nominal UTC send day — identical bucketing to the per-account queue breakdown, so the two ops surfaces agree for the same pending steps). The volume projection is capacity-INDEPENDENT and bounded by the sequence structure — not a backlog÷capacity approximation. Fails loud (500) on any missing source; no silent zero fallbacks.",
        "responses": {
          "200": {
            "description": "Sending forecast",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/SendingForecastResponse"
                }
              }
            }
          },
          "401": {
            "description": "Unauthorized"
          },
          "500": {
            "description": "Server error (e.g. shared workspace key unavailable)",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Error"
                }
              }
            }
          }
        }
      }
    },
    "/internal/audit/account-health": {
      "get": {
        "summary": "Per-account deliverability health — identity, sending config, blocked state",
        "description": "Platform-scoped (no org). Returns every sending account with its identity (email/domain), sending config (status, warmup Health Score, daily send limit), and lifecycle state (lifecycleStatus/lifecycleReason/lifecycleUpdatedAt + blocked + blockReason, from the SAME auto-derived lifecycle the live send path reads: send-eligible ⇔ lifecycle_status == 'in_production'). `inboxPlacement` is the latest inbox/spam/missing breakdown from our own Bronze/Silver/Gold placement history (recurring inbox-placement tests promoted to silver, latest test per account blended across ESP); null when the account has never been in a test. The Instantly V2 API exposes no standing per-account placement property — this figure is derived from real test results, never fabricated. Fails loud (500) on any missing REQUIRED source (account list); no silent fallbacks.",
        "responses": {
          "200": {
            "description": "Per-account deliverability health",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/AccountHealthResponse"
                }
              }
            }
          },
          "401": {
            "description": "Unauthorized"
          },
          "500": {
            "description": "Server error (e.g. shared workspace key unavailable)",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Error"
                }
              }
            }
          }
        }
      }
    },
    "/internal/audit/account-health/history": {
      "get": {
        "summary": "Per-account inbox-placement history (blended per test, newest first)",
        "description": "Platform-scoped (no org). Returns the inbox-placement history for one sending account (`email` query param, required) — one blended inbox/spam/missing entry per inbox-placement test, newest first, from our silver placement results. Empty history when the account has never been in a test.",
        "parameters": [
          {
            "schema": {
              "type": "string",
              "description": "Sending account email (required)"
            },
            "required": true,
            "description": "Sending account email (required)",
            "name": "email",
            "in": "query"
          }
        ],
        "responses": {
          "200": {
            "description": "Per-account placement history",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/AccountHealthHistoryResponse"
                }
              }
            }
          },
          "400": {
            "description": "Missing email query param",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Error"
                }
              }
            }
          },
          "401": {
            "description": "Unauthorized"
          },
          "500": {
            "description": "Server error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Error"
                }
              }
            }
          }
        }
      }
    },
    "/internal/audit/emails-backfill": {
      "post": {
        "summary": "Mirror the whole Instantly Unibox into bronze",
        "description": "Platform-scoped (no org). Walks `GET /emails` with no campaign filter and mirrors every email — sent and received — into `instantly_emails_raw`. Cancelling an Instantly plan or a single inbox permanently deletes those conversations, replies included, and silver records only THAT a lead replied, never what they wrote. Read-only against Instantly (spends no quota, declares no cost). Idempotent: a re-run re-reads pages but writes only what is new. A deploy recreates the container and kills the sweep, so the summary and each progress line report the cursor to resume from — pass it back as `startingAfter`. 202 + background; watch logs for `emails-backfill: done`.",
        "requestBody": {
          "required": false,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/EmailsBackfillRequest"
              }
            }
          }
        },
        "responses": {
          "202": {
            "description": "Accepted — backfill runs in the background",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/EmailsBackfillAccepted"
                }
              }
            }
          },
          "401": {
            "description": "Unauthorized"
          }
        }
      }
    },
    "/internal/audit/placement-test/sync": {
      "post": {
        "summary": "Poll Instantly placement tests + analytics → promote to silver",
        "description": "Platform-scoped (no org). Polls every Instantly inbox-placement test and its analytics rows, mirrors them to bronze, and promotes to silver so account-health + history reflect the latest results. Read-only against Instantly (spends no test quota). 202 + background; watch logs for `placement-sync: done`.",
        "responses": {
          "202": {
            "description": "Accepted — sync runs in the background",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PlacementSyncAccepted"
                }
              }
            }
          },
          "401": {
            "description": "Unauthorized"
          }
        }
      }
    },
    "/internal/audit/placement-test/run": {
      "post": {
        "summary": "Run one one-time inbox-placement test now (plan-compatible)",
        "description": "Platform-scoped (no org). Creates ONE one-time (type 1) fleet inbox-placement test that runs immediately — the plan-compatible recurring path (the cron calls this every 6h). Automated (type 2) tests are HyperGrowth-gated (see /ensure); one-time tests run on the Growth Inbox Placement sub. SPENDS Growth-sub test quota → gated behind PLACEMENT_TESTS_ENABLED=true (returns 409 when disabled). Fails loud (500) on a create rejection (402 quota / 400).",
        "responses": {
          "200": {
            "description": "One-time test created",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PlacementRunResponse"
                }
              }
            }
          },
          "401": {
            "description": "Unauthorized"
          },
          "409": {
            "description": "Placement testing disabled (PLACEMENT_TESTS_ENABLED != true)",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Error"
                }
              }
            }
          },
          "500": {
            "description": "Server error (e.g. Instantly 402 quota / 400)",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Error"
                }
              }
            }
          }
        }
      }
    },
    "/internal/audit/placement-test/ensure": {
      "post": {
        "summary": "Ensure the recurring automated inbox-placement tests exist",
        "description": "Platform-scoped (no org). Ensures PLACEMENT_TESTS_PER_DAY automated (type 2) inbox-placement tests exist, staggered across the day, so Instantly runs the fleet placement test on a schedule server-side. Idempotent (creates only the missing ones). SPENDS Growth-sub test quota → gated behind PLACEMENT_TESTS_ENABLED=true (returns 409 when disabled). Fails loud (500) on a create rejection (402 quota / 400).",
        "responses": {
          "200": {
            "description": "Schedule ensured",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PlacementEnsureResponse"
                }
              }
            }
          },
          "401": {
            "description": "Unauthorized"
          },
          "409": {
            "description": "Placement scheduling disabled (PLACEMENT_TESTS_ENABLED != true)",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Error"
                }
              }
            }
          },
          "500": {
            "description": "Server error (e.g. Instantly 402 quota / 400)",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Error"
                }
              }
            }
          }
        }
      }
    },
    "/internal/audit/seed-placement/run": {
      "post": {
        "summary": "Dispatch one in-house seed inbox-placement test",
        "description": "Platform-scoped (no org). Sends one seed email from every testable mailbox to every receiver mailbox we own and can read over IMAP — the self-hosted replacement for Instantly's paid Growth Inbox Placement subscription, which it spends nothing to do. SENDS REAL MAIL from the fleet, so it is gated behind SEED_PLACEMENT_ENABLED=true (409 when disabled). Results are read by the SEPARATE /seed-placement/sync run on its own schedule — never chained here, or the read would run before the seeds land. 202 + background; watch logs for `seed-placement-run: done`.",
        "requestBody": {
          "required": false,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/SeedPlacementRunRequest"
              }
            }
          }
        },
        "responses": {
          "202": {
            "description": "Accepted — the seed dispatch runs in the background",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PlacementSyncAccepted"
                }
              }
            }
          },
          "401": {
            "description": "Unauthorized"
          },
          "409": {
            "description": "Seed placement disabled (SEED_PLACEMENT_ENABLED != true)",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Error"
                }
              }
            }
          }
        }
      }
    },
    "/internal/audit/seed-placement/sync": {
      "post": {
        "summary": "Read the seeds back and promote them to placement silver",
        "description": "Platform-scoped (no org). Opens every receiver mailbox that still owes an observation, records which folder each seed landed in (inbox vs spam), and promotes the affected tests into the SAME `instantly_placement_results` silver the Instantly path writes — so the lifecycle delivery gate and account-health read them with no code change. Read-only against the mailboxes and idempotent, so it is NOT behind the kill-switch. 202 + background; watch logs for `seed-placement-sync: done`.",
        "responses": {
          "202": {
            "description": "Accepted — the read + promotion runs in the background",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PlacementSyncAccepted"
                }
              }
            }
          },
          "401": {
            "description": "Unauthorized"
          }
        }
      }
    },
    "/internal/infra/sync": {
      "post": {
        "summary": "Poll every infrastructure provider → bronze + silver inventory",
        "description": "Platform-scoped (no org). Polls Gandi (three organisations), Mailforge, Primeforge and Instantly DFY for the domains and mailboxes we own, mirrors each vendor payload to bronze, upserts the canonical silver inventory, and flags rows a vendor stopped reporting (never deletes them). Read-only against every vendor and free of metered spend — these are flat-subscription inventory reads, so no run or cost is declared. A single vendor failing is counted and logged without stopping the others; the run throws only when every vendor failed. 202 + background; watch logs for `infra-sync: done`.",
        "responses": {
          "202": {
            "description": "Accepted — sync runs in the background",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/InfraSyncAccepted"
                }
              }
            }
          },
          "401": {
            "description": "Unauthorized"
          }
        }
      }
    },
    "/internal/infra/domains": {
      "get": {
        "summary": "Domain inventory across all four infrastructure vendors",
        "description": "Platform-scoped (no org). One row per (provider, domain): who we buy it from, when it expires, how many mailboxes the vendor hosts, how many Instantly accounts send from it, and what it costs per month and per email. The vendor mailbox count and the Instantly account count are deliberately both shown and routinely differ — the legacy relayed domains run dozens of accounts against a single vendor mailbox. Every amount carries its provenance; a domain nothing prices reports null.",
        "responses": {
          "200": {
            "description": "Inventory",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/InfraDomainsResponse"
                }
              }
            }
          },
          "401": {
            "description": "Unauthorized"
          },
          "500": {
            "description": "Server error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Error"
                }
              }
            }
          }
        }
      }
    },
    "/internal/infra/waste": {
      "get": {
        "summary": "Domains billed for but unused, cancelled, or expiring",
        "description": "Platform-scoped (no org). REPORT-ONLY by design: it never cancels an autorenew and never schedules a deletion. A false positive costs us a domain, and several flagged domains are brand domains held on purpose — a human decides.",
        "responses": {
          "200": {
            "description": "Findings",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/InfraWasteResponse"
                }
              }
            }
          },
          "401": {
            "description": "Unauthorized"
          },
          "500": {
            "description": "Server error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Error"
                }
              }
            }
          }
        }
      }
    },
    "/internal/infra/spend": {
      "get": {
        "summary": "Monthly infrastructure run-rate by vendor",
        "description": "Platform-scoped (no org). What the vendors charge US — deliberately separate from costs-service, which prices what we RE-BILL the customer; the difference between the two is the real margin per email. Totals stay PER CURRENCY (Gandi bills in EUR, the rest in USD) because blending them would need an FX rate this service does not own. `unpricedProviders` is the honest hole, so cost-per-email is a FLOOR while it is non-empty.",
        "responses": {
          "200": {
            "description": "Spend summary",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/InfraSpendResponse"
                }
              }
            }
          },
          "401": {
            "description": "Unauthorized"
          },
          "500": {
            "description": "Server error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Error"
                }
              }
            }
          }
        }
      }
    }
  }
}